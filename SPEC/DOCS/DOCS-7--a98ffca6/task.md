# DOCS-7: Doc-truth sweep: enrolment is invite-gated (11 passages, 5 files)

| Field | Value |
| --- | --- |
| Public id | `a98ffca6-7b87-44c4-a0bd-26f12894a1f3` |
| Key | DOCS-7 |
| Epic | [DOCS](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-16T08:37:02.760743+00:00 |
| Updated | 2026-08-16T08:37:02.760743+00:00 |
| Completed | — |

## Description

FILED 2026-08-16 from the doc-truth deep dive (DOC-TRUTH_DEEPDIVE.md, section 8, item T3).
Operator-requested audit: "look for contradictions between what's built / implemented and what's
documented (including comments)".

# What is wrong
Doc-truth sweep: enrolment is invite-gated (11 passages, 5 files)

# Scope
`CONTRACTS-HTTP.md`, `CONTRACTS-CLI.md`, `CONTRACTS-ONDISK.md`, `internal/invite/doc.go`, `internal/auth/roster.go`, `client/enrol.go`

# Proof sketch (from the audit; MUST be observed RED before the fix)
grep pinning each specific line; RED before

# Why this class matters here
The audit found ~76 contradictions that collapse into FIVE false narratives, each asserted 3-17
times across independent files: enrolment is not invite-gated (16x, false since 3cedcb7), relay is
registered on no mux / imported by nothing (17x, false -- it is mounted and live), the client
verifies signatures on read (3x, nothing verifies), the bus does not request a client certificate
(2x, it is tls.RequestClientCert), and main.go injects the memory roster (3x, durable since AUTH-7).

The multiplication is the danger: a reader who cross-checks two independent files gets MUTUAL
CONFIRMATION of a falsehood, which reads as stronger evidence than a single claim would.

This is the CONTEXT-STALE-NOTYET class (67b42913): text written truthfully, never linked to the
commit that closed the gap, now read as freshly checked.

# Standard of evidence
A doc proof must PIN THE SPECIFIC LINE it claims to prove and must be OBSERVED RED BEFORE THE FIX.
A grep proof that passes on an incidental match elsewhere in the file has already green-lit a bad
close in this repo. Run it through `bash scripts/proof-check.sh` and quote the verdict.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [AUTH-7](../../AUTH/AUTH-7--4ba67a7b/task.md) — AUTH-7: Operator/admin can clear one agent's active sessions without restarting the bus (todo)
- [CONTEXT-STALE-NOTYET](../../CONTEXT/CONTEXT-STALE-NOTYET--67b42913/task.md) — CONTEXT-STALE-NOTYET: a doc-check \`forbid\` mode, so a "not yet implemented" note cannot o… (todo)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
