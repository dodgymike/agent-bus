# DOCS-4: CLAUDE.md is stale on invariant 3 -- enrolment IS invite-gated at HEAD

| Field | Value |
| --- | --- |
| Public id | `a24c33cd-976d-477f-9250-336983c69ec2` |
| Key | DOCS-4 |
| Epic | [DOCS](../epic.md) |
| Status | done |
| Priority | P3 |
| Component | docs |
| Section | backlog |
| Tags | claude-md, invariant-3, stale-doc |
| Created | 2026-08-15T14:15:49.652588+00:00 |
| Updated | 2026-08-15T14:45:17.503736+00:00 |
| Completed | 2026-08-15T14:45:17.503718+00:00 |

## Proof command

```sh
bash -c 'fail=0; for f in CLAUDE.md AGENTS.md; do if grep -qF "NOT yet invite-gated" "$f"; then echo "STALE invariant-3 claim still in $f"; fail=1; fi; if grep -qF "Enrolment IS invite-gated as of" "$f"; then :; else echo "corrected invite-gate statement MISSING from $f"; fail=1; fi; done; for c in "REQUESTS but never" "CANNOT verify message signatures" "idempotency is in-memory only"; do if grep -qF "$c" CLAUDE.md; then :; else echo "still-true caveat WRONGLY REMOVED from CLAUDE.md: $c"; fail=1; fi; done; if [ "$fail" -ne 0 ]; then echo DOCS4_STALE; exit 1; fi; echo DOCS4_OK'
```

## Description

CLAUDE.md's invariant-status preamble states enrolment is NOT yet invite-gated (`InviteRequired: false`). At HEAD, cmd/agent-bus/main.go:66 has `enrolmentInviteRequired = true`, the gate is wired through to a 403 (internal/httpapi/auth.go), and TestInviteGateEnrolWithoutAnInviteIsRefused403 covers it. Found by the DEPLOY-6 code reviewer while re-verifying invariant 3 claims against HEAD; deliberately NOT fixed in DEPLOY-6 because CLAUDE.md is configuration and outside that task's file boundary.

Wanted: edit CLAUDE.md in place to remove the now-false 'enrolment is NOT yet invite-gated' clause from the invariant-status preamble (the paragraph above invariant list that begins 'These state what MUST be true, not what IS true today'), leaving the remaining true caveats (client cert requested-not-required, recipients cannot verify message signatures, enrol idempotency in-memory only) intact. This is an edit-in-place, NOT an addition -- CLAUDE.md has a byte ceiling recorded in docs/doc-budgets.tsv, so the edit must not grow the file, or must trim elsewhere to stay under budget.

Proof must be a doc proof that is RED before the fix (grep still finds the false 'NOT yet invite-gated' claim) and GREEN after (grep finds no such claim, and finds the corrected/removed text) -- per CLAUDE.md's own warning that grep-based doc proofs must pin the specific line and be observed RED first.

Reference: DEPLOY-6 (reviewer finding, task e12b75cd-c17e-4f73-b4b5-0b04dd868455).

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


- **follow-up of** [DOCS-4-FU-BUDGET](../../CONTEXT/DOCS-4-FU-BUDGET--721b51ef/task.md)

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [DEPLOY-6](../../DEPLOY/DEPLOY-6--e12b75cd/task.md) — DEPLOY-6: host-reachable Dockerfile CMD + THREE-BUS-DOCKER.md federated runbook (done)

## Referenced by other tasks (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [DOCS-4-FU-BUDGET](../../CONTEXT/DOCS-4-FU-BUDGET--721b51ef/task.md) — DOCS-4-FU-BUDGET: aade191 grew CLAUDE.md 678 bytes past its own recorded 28781-byte ratch… (todo)
- [INVITE-GATE-ENFORCE-FU-ENROLGODOC](../../INVITE/INVITE-GATE-ENFORCE-FU-ENROLGODOC--e2ede3a4/task.md) — INVITE-GATE-ENFORCE-FU-ENROLGODOC: client/enrol.go godoc on the Invite field still tells… (todo)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
