# DOCS-3: CONTRACTS.md -- route/flag/env-var/record-type table

| Field | Value |
| --- | --- |
| Public id | `a24bb214-dba0-421e-ae17-7148060acf72` |
| Key | DOCS-3 |
| Epic | [DOCS](../epic.md) |
| Status | todo |
| Priority | P1 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-02T09:05:59.126012+00:00 |
| Updated | 2026-08-02T20:51:28.810945+00:00 |
| Completed | — |

## Proof command

```sh
test -s CONTRACTS-CLI.md && test -s CONTRACTS-HTTP.md && test -s CONTRACTS-ONDISK.md && test -s CONTRACTS-AGENT.md && grep -qF "CONTRACTS-CLI.md" CONTRACTS.md && grep -qF "CONTRACTS-HTTP.md" CONTRACTS.md && grep -qF "CONTRACTS-ONDISK.md" CONTRACTS.md && grep -qF "CONTRACTS-AGENT.md" CONTRACTS.md
```

## Status note

2026-08-02: proof_cmd strengthened. Old proof (`test -s CONTRACTS.md`) was near-vacuous post-split -- trivially true of a 36-line index file, asserted nothing about content. JUDGEMENT: did NOT supersede this task, even though CONTRACTS-SPLIT (360a2679) has arguably fulfilled its intent, because 360a2679 is still status=in_progress and UNCOMMITTED (files are `git add`ed but not committed) -- closing DOCS-3 against uncommitted work would repeat the committed!=running trap in a different guise (committed!=merged/landed). Instead strengthened the proof to assert REAL content: all four plane files (CONTRACTS-CLI/HTTP/ONDISK/AGENT.md) are non-empty AND the index (CONTRACTS.md) links to all four by name. Ran through proof-check.sh: verdict=PASS class=file-assertion exit=0 (GREEN today, since the split content already exists on disk pre-commit). Recommend: once 360a2679 is committed, reconsider superseding DOCS-3 via a supersedes relation from 360a2679 -- leaving that call to whoever closes 360a2679, since a task should not be superseded by work that has not itself landed.

## Description

A single table of every route, CLI flag, env var, and durable record type, with the convention that every future task updates it in the same commit that changes any of those surfaces (CLAUDE.md step 9).

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [CONTRACTS-SPLIT](../CONTRACTS-SPLIT--360a2679/task.md) — CONTRACTS-SPLIT: split CONTRACTS.md into per-plane files (pure move) + retarget every pro… (done)

## Referenced by other tasks (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [CONTRACTS-SPLIT](../CONTRACTS-SPLIT--360a2679/task.md) — CONTRACTS-SPLIT: split CONTRACTS.md into per-plane files (pure move) + retarget every pro… (done)
- [CRYPTO-12](../../CRYPTO/CRYPTO-12--eb1827ff/task.md) — CRYPTO-12: PROTOCOL.md wire format + CONTRACTS.md for the crypto surface (todo)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
