# AGENT_LOG entry + skipped-doc-gate justification for the 2026-08-14 internal/idem comment-narrowing and cross-agent crash-test pass

| Field | Value |
| --- | --- |
| Public id | `ff38f871-988a-4f2c-aa9a-febee4f3b15a` |
| Key | _(null in the export)_ |
| Epic | [DOCS](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-14T23:04:09.736542+00:00 |
| Updated | 2026-08-14T23:04:09.736542+00:00 |
| Completed | — |

## Proof command

```sh
grep -q 'IDEM-17-FU-CROSSAGENT' AGENT_LOG.md && grep -q 'RELAY-FU-IDEM-METER-BY-PEER' AGENT_LOG.md && grep -qi 'documentation agent' AGENT_LOG.md && echo AGENTLOG_ENTRY_PRESENT
```

## Description

Filed 2026-08-14 by spec-keeper on the feature-runner's report. The feature-runner completed two increments this pass but could NOT write the mandated paper trail: AGENT_LOG.md and DECISIONS.md are OUTSIDE its file-ownership boundary (internal/idem/ only) and are shared append-only files owned by other LIVE agents this session -- CLAUDE.md's parallel-agent rule says one agent at a time on those. So the entry is UNWRITTEN, and this task carries it rather than leaving it to be forgotten.

TWO THINGS TO APPEND, both to AGENT_LOG.md, in ONE new dated section (prefer adding a new dated section over editing existing lines):

(a) THE AGENT_LOG ENTRY for the two increments of that pass:
    - RELAY-FU-IDEM-METER-BY-PEER in-package residue (comment-only): internal/idem/store.go, errors.go, scope.go stopped asserting that the fair-share bucket key "is therefore a PROVEN IDENTITY, not an attacker-chosen label", that a share refusal is "always SELF-INFLICTED", and that an operator should "reach for the one client named in the message" -- all three FALSE on the relay ingest path. The TASK REMAINS OPEN AT P0; only the false prose was fixed.
    - IDEM-17-FU-CROSSAGENT (test-only): internal/idem/crashinjection_test.go gained TestIdemCrashInjectionRestartCrossAgentKeyIsolation, closing the security gate's P2-4 post-restart cross-agent oracle, with six guards each mutation-tested RED alone on a distinct line.
    Both were code-complete and NOT committed by the feature-runner; take the sha(s) from the integrator's actual commit(s), and do not write a sha that does not exist.

(b) THE JUSTIFICATION LINE CLAUDE.md step 10 REQUIRES for the documentation agent not running as a separate step. The honest justification, which this task exists to put on the record rather than leave implied: the change IS documentation -- Go doc comments -- and it touches NO route, NO flag, NO env var, NO on-disk format and NO agent-facing surface, so no CONTRACTS-*.md plane moved and AGENT_PROTOCOL.md is untouched. Write that line explicitly; a skipped gate with no recorded reason is the thing the rule is guarding against.

NO DECISIONS.md ENTRY IS OWED BY THIS PASS -- nothing was decided that weakens an invariant; the comment narrowing brought the prose back INTO line with the invariants rather than away from them. (Separately, RELAY-FU-IDEM-METER-BY-PEER's eventual FIX does owe a DECISIONS.md entry; that obligation is recorded on that task, not here. Do not conflate the two.)

SCOPE GUARD: this task appends to AGENT_LOG.md only. It must NOT be used to fix the DECISIONS.md / CONTRACTS-HTTP.md contradictions -- those are IDEM-11-FU-PAPERTRAIL (c416a458-0bde-4a9b-8818-5dfe11efb48e) and are a different, live defect.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [IDEM-11-FU-PAPERTRAIL](../IDEM-11-FU-PAPERTRAIL--c416a458/task.md) — IDEM-11-FU-PAPERTRAIL: DECISIONS.md and CONTRACTS-HTTP.md state the OPPOSITE of what IDEM… (todo)
- [IDEM-17-FU-CROSSAGENT](../../IDEM/IDEM-17-FU-CROSSAGENT--0cd0ce79/task.md) — Crash-injection coverage for cross-agent applied-key isolation across recovery (todo)
- [RELAY-FU-IDEM-METER-BY-PEER](../../RELAY/RELAY-FU-IDEM-METER-BY-PEER--8774f265/task.md) — RELAY-FU-IDEM-METER-BY-PEER: Meter the applied-key table by the AUTHENTICATED PEER, not t… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
