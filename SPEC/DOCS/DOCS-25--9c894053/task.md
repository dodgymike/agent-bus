# DOCS-25: \`CONTRACTS-AGENT.md\` documents the log-scrape that \`bus-serve.sh\` deliberately refuses

| Field | Value |
| --- | --- |
| Public id | `9c894053-1f04-4536-bf2c-aad298e4089f` |
| Key | DOCS-25 |
| Epic | [DOCS](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-16T08:37:33.936196+00:00 |
| Updated | 2026-08-16T08:37:33.936196+00:00 |
| Completed | — |

## Description

FILED 2026-08-16 from the doc-truth deep dive (DOC-TRUTH_DEEPDIVE.md section 8, item T21).
Operator-requested audit: contradictions between what is BUILT and what is DOCUMENTED (incl. comments).

# What is wrong
`CONTRACTS-AGENT.md` documents the log-scrape that `bus-serve.sh` deliberately refuses

# Scope
`CONTRACTS-AGENT.md` HEAD:48 (+ `:19` count, `:29`/`:104` fed-smoke) — **security-relevant: it describes a trust-anchor substitution the script exists to prevent**

# Proof sketch -- MUST be observed RED before the fix
grep pins the corrected sentence; `git ls-files scripts/ \ | wc -l` matches the stated count

# Why this class matters
~76 contradictions collapse into FIVE false narratives, each asserted 3-17 times across INDEPENDENT
files: enrolment is not invite-gated (16x; false since 3cedcb7), relay is registered on no mux /
imported by nothing (17x; it is mounted and live), the client verifies signatures on read (3x;
nothing verifies), the bus does not request a client certificate (2x; tls.RequestClientCert), and
main.go injects the MEMORY roster (3x; durable since AUTH-7).

The multiplication is the danger: a reader cross-checking two independent files gets MUTUAL
CONFIRMATION of a falsehood, which reads as stronger evidence than one claim would.
CONTEXT-STALE-NOTYET class (67b42913): written truthfully, never linked to the commit that closed
the gap, now read as freshly checked.

# Standard of evidence
Pin the SPECIFIC line. A grep proof passing on an incidental match elsewhere in the file has already
green-lit a bad close here. Observe RED before the fix; run via scripts/proof-check.sh and quote the
verdict.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [AUTH-7](../../AUTH/AUTH-7--4ba67a7b/task.md) — AUTH-7: Operator/admin can clear one agent's active sessions without restarting the bus (todo)
- [CONTEXT-STALE-NOTYET](../../CONTEXT/CONTEXT-STALE-NOTYET--67b42913/task.md) — CONTEXT-STALE-NOTYET: a doc-check \`forbid\` mode, so a "not yet implemented" note cannot o… (todo)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
