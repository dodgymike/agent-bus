# DOCS-4-FU-BUDGET: aade191 grew CLAUDE.md 678 bytes past its own recorded 28781-byte ratchet, and nothing enforces the ceiling

| Field | Value |
| --- | --- |
| Public id | `721b51ef-8daf-421b-b56e-3fb77a17f7cf` |
| Key | DOCS-4-FU-BUDGET |
| Epic | [CONTEXT](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-15T14:47:33.862687+00:00 |
| Updated | 2026-08-15T14:47:33.862687+00:00 |
| Completed | — |

## Proof command

```sh
TBD by implementer -- must be observed RED against the current 29459-byte CLAUDE.md under the 28781 ceiling in docs/doc-budgets.tsv, and GREEN after the chosen remedy; if the ceiling is re-baselined instead of the file trimmed, the proof must also assert the recorded reason is present so a bare number-bump cannot pass
```

## Description

Filed 2026-08-15 by spec-keeper while closing DOCS-4. MEASURED, not estimated.

DOCS-4 required the invariant-3 correction to be an EDIT IN PLACE: "the edit must not grow the file, or must trim elsewhere to stay under budget." The commit that discharged it, `aade191`, instead ADDED explanation:

    CLAUDE.md at 1753121 (aade191's parent):  28781 bytes
    CLAUDE.md at aade191 and at HEAD 9938eb2: 29459 bytes   (+678)

`docs/doc-budgets.tsv` records the CLAUDE.md ceiling as **28781** -- exactly the pre-aade191 size, set deliberately as a RATCHET ("its ceiling is set AT its current size: a ratchet, not headroom"). So HEAD overruns its own recorded ceiling by 678 bytes.

DOCS-4 was completed anyway, deliberately and on the record: its OUTCOME (the false "enrolment is NOT yet invite-gated" claim is gone from CLAUDE.md and AGENTS.md) is fully delivered and was verified RED-at-parent / GREEN-at-HEAD. Keeping it open could not undo a commit that is already history. This task carries the residue.

== TWO THINGS TO SETTLE, IN THIS ORDER ==

1. **Is 28781 still the right ceiling?** `docs/doc-budgets.tsv` is UNTRACKED at HEAD -- it is a live proposal from CONTEXT-DOCCHECK, not yet a committed authority -- and it was written against a tree where CLAUDE.md was 28781. The +678 bought something real: an accurate, dated statement of which invariants are enforced, which is exactly the kind of text the budget file's own comments say must NOT be deleted to make room. So the honest options are (a) trim 678+ bytes of genuinely lower-value text from CLAUDE.md, or (b) re-baseline the ceiling to 29459 WITH a recorded reason, per the file's own rule that "raising it needs a recorded reason". Do NOT silently raise it, and do NOT delete the reasoning paragraphs to fit.

2. **The ratchet is currently unenforced.** Nothing in the check loop runs `scripts/doc-check.sh budget`, and doc-check.sh is itself untracked, so this overrun happened invisibly. Whichever way (1) is settled, the ceiling only means something once the budget check runs somewhere that fails a commit.

Belongs to the CONTEXT epic, whose stated job is LOWERING this number.

== PROOF ==

TBD by implementer, and it must be genuine: it has to demonstrate the check goes RED against the current 29459-byte CLAUDE.md under the recorded ceiling and GREEN after whichever remedy is chosen. If option (b) is taken, the proof must additionally assert the recorded reason is present, so a bare number-bump cannot pass. Per CLAUDE.md, a `grep`/threshold proof must be observed RED before the fix -- a proof never seen failing is not evidence.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


- **follow-up** [DOCS-4](../../DOCS/DOCS-4--a24c33cd/task.md)

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [CONTEXT-DOCCHECK](../CONTEXT-DOCCHECK--b3b28f45/task.md) — CONTEXT-DOCCHECK: doc-check.sh -- the instrument every other proof in this epic depends on (todo)
- [DOCS-4](../../DOCS/DOCS-4--a24c33cd/task.md) — DOCS-4: CLAUDE.md is stale on invariant 3 -- enrolment IS invite-gated at HEAD (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
