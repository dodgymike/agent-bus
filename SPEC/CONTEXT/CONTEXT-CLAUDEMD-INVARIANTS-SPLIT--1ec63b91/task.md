# CONTEXT-CLAUDEMD-INVARIANTS-SPLIT: retroactively file and close the CLAUDE.md/INVARIANTS.md split + SPEC.md mirror filter (committed 711e282, no task filed)

| Field | Value |
| --- | --- |
| Public id | `1ec63b91-0b63-4d05-99ef-77c427841265` |
| Key | _(null in the export)_ |
| Epic | [CONTEXT](../epic.md) |
| Status | done |
| Priority | P2 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-08T15:47:00.578856+00:00 |
| Updated | 2026-08-08T15:48:17.390329+00:00 |
| Completed | 2026-08-08T15:48:17.390313+00:00 |

## Proof command

```sh
[ "$(grep -c "^### Invariant " INVARIANTS.md)" = 11 ] && ! grep -qi "scripts/bus-\*\.sh.*the ONLY interface agents use" CLAUDE.md && [ "$(grep -c "^- \[x\]" SPEC.md)" = 0 ] && echo PROOF_OK
```

## Description

RETROACTIVE TASK (filed 2026-08-08 by spec-keeper, at the orchestrator's request). Work was already
committed at 711e282 with no Spec Server task filed for it -- the orchestrator (main) did it directly
at the user's request. This closes that audit gap: no proof_cmd, no recorded gate verdict, nothing to
check the commit against, until now. An integrator searched for a matching task before committing,
found none, and proceeded on independent verification -- correctly, but it should not have had to.

RELATIONSHIP TO CONTEXT-CLAUDE-TRIM (6ef1d88e): this is NOT that task executed early. 6ef1d88e's own
definition of done is moving the AGENT ROSTER DESCRIPTIONS and MODEL-SELECTION RATIONALE out of
CLAUDE.md into a new .claude/ORCHESTRATION.md -- untouched by 711e282. What 711e282 DID satisfy is
6ef1d88e's own stated HARD PRE-REQUISITE: "the in-flight CLAUDE.md split (rule inline, rationale to
INVARIANTS.md) must be COMMITTED before this task starts." That prerequisite was apparently in flight
as untracked work; this task is what closes it out with a record. 6ef1d88e itself remains open and
unmodified in scope (see reconciliation note posted there).

WHAT SHIPPED (commit 711e282, epic CONTEXT):
1. CLAUDE.md's invariants section (195 of 461 lines) extracted PROGRAMMATICALLY (sed -n '14,206p')
   into a new INVARIANTS.md, verified byte-identical to the extraction apart from 11 added
   "### Invariant N -- <title>" headings: stripping those headings and re-diffing yields 11 pure
   blank-line insertions, zero deletions, zero modifications. CLAUDE.md now carries the one-line
   rules plus an imperative pointer to read the relevant invariant in INVARIANTS.md before touching
   that plane. Net: CLAUDE.md 461 -> 369 lines in the current tree (measured at HEAD, not the
   commit's own stated 363 -- later CONTEXT/DUR work has touched CLAUDE.md since).
2. New scripts/gen-spec-mirror.sh regenerates SPEC.md filtered to OPEN tasks only (closed tasks were
   39% of the backlog). Fails closed rather than emit a corrupted mirror: refuses to write on an
   empty/failed export, on an export that doesn't look like the markdown mirror, and on a structural
   column-0 assertion (added in reviewer pass 2) that catches a shape the count-based guard alone
   cannot see -- a stray column-0 markdown heading or checklist marker inside a task description.
3. .claude/agents/spec-keeper.md updated to point at the new script instead of the bare
   `spec-cloud.sh ... export > SPEC.md`, which restored closed tasks and could silently overwrite
   SPEC.md with an error page on a failed fetch.

GATES. Reviewer ran COMPLETED across TWO PASSES -- pass 1 CHANGES-REQUESTED with six findings, pass 2
PASS with two must-fix items, both addressed (see kind=response notes below, and re-verified against
the tree at HEAD by spec-keeper: the CLAUDE.md verify-rule wording, the invariant 4/6 narrowing
cross-reference, and the gen-spec-mirror.sh comment's coverage claim all read correctly post-fix).
NO SECURITY GATE RAN -- recorded honestly, with the reason: the change ships one shell script plus
two markdown-only files and no server code, and the user has separately deferred non-relay security
work (see the SSH-tunnel decision on the RELAY epic notes, same date).

FILE OWNERSHIP: CLAUDE.md, INVARIANTS.md (new), scripts/gen-spec-mirror.sh (new),
.claude/agents/spec-keeper.md.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [CONTEXT-CLAUDE-TRIM](../CONTEXT-CLAUDE-TRIM--6ef1d88e/task.md) — CONTEXT-CLAUDE-TRIM: the agent roster descriptions and model-selection rationale leave CL… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
