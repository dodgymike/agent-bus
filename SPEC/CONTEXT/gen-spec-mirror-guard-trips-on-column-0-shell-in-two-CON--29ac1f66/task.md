# gen-spec-mirror guard trips on column-0 shell in two CONTEXT-epic task descriptions, blocking regen

| Field | Value |
| --- | --- |
| Public id | `29ac1f66-efb9-4b05-afda-a2775e50f1c6` |
| Key | _(null in the export)_ |
| Epic | [CONTEXT](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | tooling |
| Section | backlog |
| Tags | — |
| Created | 2026-08-22T13:09:35.809928+00:00 |
| Updated | 2026-08-22T13:09:35.809928+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/gen-spec-mirror.sh --no-relations 2>&1 | grep -v "REFUSING TO WRITE" | grep -q "Regenerate\|wrote\|SPEC"
```

## Description

BUG (found by spec-keeper, 2026-08-22, while regenerating the mirror after completing task 9ef57953). `bash scripts/gen-spec-mirror.sh` REFUSES TO WRITE: `unexpected non-blank column-0 lines: 22`, and SPEC.md/SPEC/ are unchanged (guard is working as designed -- see gen-spec-mirror.sh comment block, guard 1). The last successful regen was commit dbae79d (757 tasks); the server now has 840, so the mirror is stale by 83 tasks pending this fix.

ROOT CAUSE: two tasks embed multi-line shell scripts in their description's `_Proof:` field starting at column 0 (not indented under the bullet), so the structural awk check in gen-spec-mirror.sh flags every unindented line inside those blocks as an unexpected column-0 line:
  - 5f3a3cd6-f53a-4bb4-a99f-6fe44b3791ac (CONTEXT-DOCCHECK-FU-COUNTPIN) -- 12 offending lines
  - d7277d6f-6a0a-484a-8cd9-cf5f5247446c (CONTEXT-DOCCHECK-SYMLINK) -- 10 offending lines

Both are `_Proof:` bodies containing a `bash -c '...'` or `T=$(mktemp -d); ...` shell snippet whose interior lines (if/then/fi, sed, cd, etc.) sit flush left in the raw markdown export, matching none of the guard's permitted column-0 shapes (`# `, `#{2,6} `, `- [.] `, `> `).

FIX: re-indent the offending description text (spec-keeper metadata edit via PATCH on each task's `description`, preserving content exactly, just indenting the embedded shell lines under the bullet/continuation the way other multi-line proof blocks in the same export already are) so the raw markdown export has no stray column-0 lines. This is a description-content fix, not a script change -- the guard itself is doing its job correctly per its own documented rationale (case (a)/(b)/(c) in gen-spec-mirror.sh) and should not be loosened.

Verify: `bash scripts/gen-spec-mirror.sh` completes without the REFUSING TO WRITE message and SPEC.md/SPEC/ update to reflect the current task count.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [9ef57953-e746-4f7b-90b5-f8ddaee087f3](../../PROCESS/review-panel-wastes-work-each-reviewer-independently-re--9ef57953/task.md) — review panel wastes work: each reviewer independently re-runs the full \`go test -race ./.… (done)
- [CONTEXT-DOCCHECK-FU-COUNTPIN](../CONTEXT-DOCCHECK-FU-COUNTPIN--5f3a3cd6/task.md) — CONTEXT-DOCCHECK-FU-COUNTPIN: the outer doc-check.sh --selftest run's assertion count is… (todo)
- [CONTEXT-DOCCHECK-SYMLINK](../CONTEXT-DOCCHECK-SYMLINK--d7277d6f/task.md) — CONTEXT-DOCCHECK-SYMLINK: physical (realpath) containment for cmd_section -- a symlink st… (todo)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
