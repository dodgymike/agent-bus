# ACK-3-FU-COLLAPSE-WIREVERSION: collapse relay.AckWireVersion onto relay.WireVersion once RELAY-23 lands

| Field | Value |
| --- | --- |
| Public id | `8c6d6765-ba5e-40d7-b7ef-84cbc8bd72bf` |
| Key | ACK-3-FU-COLLAPSE-WIREVERSION |
| Epic | [ACK](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | relay |
| Section | backlog |
| Tags | — |
| Created | 2026-08-16T13:51:15.880799+00:00 |
| Updated | 2026-08-16T13:51:15.880799+00:00 |
| Completed | — |

## Description

ACK-3 declares relay.AckWireVersion / resolveAckWireVersion / ErrUnsupportedAckVersion / CodeUnsupportedAckVersion in internal/relay/ackframe.go. RELAY-23 (unmerged at the time, on branch worktree-agent-a3b41d07f84017fc1) declares relay.WireVersion / resolveWireVersion / ErrUnsupportedRelayVersion / CodeUnsupportedRelayVersion in internal/relay/message.go. Both spend the SAME reserved relay-wire-version = 1 and both implement the SAME rules (absent reads as 1; unrecognised is REFUSED never defaulted; the literal 1 must not be respelled as the constant).
ACK-3 used an ACK-scoped spelling DELIBERATELY: declaring the same name in two files of one package produces a build break git CANNOT flag as a conflict -- no overlapping text, so the merge succeeds and the package stops compiling. Once RELAY-23 has landed, collapse them onto one constant and one resolver. Decide separately whether to keep two error CODES (unsupported_ack_version / unsupported_relay_version, which tell a peer operator WHICH frame the far end could not parse) or one. Documented in CONTRACTS-ONDISK.md, 'relay-wire-version = 1 IS NOW SPENT'.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [ACK-3](../ACK-3--263c47fe/task.md) — ACK-3: Authenticated peer-hop ACK/NACK wire semantics and correlation (done)
- [RELAY-23](../../RELAY/RELAY-23--220d36f4/task.md) — RELAY-23: Relay wire protocol version (todo)

## Referenced by other tasks (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-53](../../RELAY/RELAY-53--d5bbdec9/task.md) — RELAY-53: RELAY-23 will merge cleanly and then fail to compile -- two wire-version resolv… (todo)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
