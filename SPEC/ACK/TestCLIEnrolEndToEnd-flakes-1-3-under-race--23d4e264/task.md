# TestCLIEnrolEndToEnd flakes ~1/3 under -race

| Field | Value |
| --- | --- |
| Public id | `23d4e264-a6ae-4fd6-ae4f-1a41a56097de` |
| Key | _(null in the export)_ |
| Epic | [ACK](../epic.md) |
| Status | todo |
| Priority | P1 |
| Component | test |
| Section | backlog |
| Tags | — |
| Created | 2026-08-30T20:53:45.362888+00:00 |
| Updated | 2026-08-30T20:53:45.362888+00:00 |
| Completed | — |

## Proof command

```sh
go test -race -count=20 -run "^TestCLIEnrolEndToEnd$" ./cmd/agent-busctl
```

## Description

internal/cmd/agent-busctl E2E test TestCLIEnrolEndToEnd fails non-deterministically (~1 in 3) under go test -race, even run in ISOLATION with -count=1, and on clean main HEAD (pre-existing, unrelated to conversations -- the conversation change does not touch enrol). Observed twice this session (during ACK-17 and CONV-CREATE-CLI -race runs). Root cause likely a priming-server startup/teardown race in the test harness (the failing run shows the primed server exiting under SIGTERM). A 1/3 flake rate makes every full -race ./... unreliable as a gate. Needs diagnosis + a deterministic fix (proper server-ready wait / teardown ordering).

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [ACK-17](../ACK-17--d4a2d828/task.md) — ACK-17: four keying mutations still leave internal/httpapi green -- session-keyed is the… (done)
- [CONV-CREATE-CLI](../../CONV/CONV-CREATE-CLI--627d20e0/task.md) — CONV-CREATE-CLI: mint a conversation -- HTTP route + agent-busctl subcommand + AGENT_PROT… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
