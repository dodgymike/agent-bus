# ACK-6-FU-SETTLE-ERRORLOG: throttle the terminal-conflict ERROR in ack.Store.Settle

| Field | Value |
| --- | --- |
| Public id | `dc58e7ee-aa5a-47b8-8016-8398536c78f6` |
| Key | _(null in the export)_ |
| Epic | [ACK](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | ack |
| Section | backlog |
| Tags | — |
| Created | 2026-08-16T16:01:50.604492+00:00 |
| Updated | 2026-08-16T16:01:50.604492+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/proof-check.sh 'go test -race -run "^TestSettleConflictLogIsThrottled$" ./internal/ack'
```

## Description

Found by ACK-6's security gate (MEDIUM). internal/ack/store.go, Settle's second-different-terminal branch logs at ERROR with no throttle. Until ACK-6 landed, Settle had NO CALLER (its own doc said so), so the line was unreachable. POST /v1/ack now makes it remotely drivable: an agent that legitimately ACKed one of its own messages can emit one unthrottled ERROR line PER REQUEST for the whole 24h retention window. That is precisely the failure internal/hub/hub.go recordAcceptance documents at length and that ack.Store.refuseLocked already defends against for the capacity path -- an OBSERVABILITY table becoming a DISK outage. FIX: give the terminal-conflict log refuseLocked's throttle shape (one line per minute, with a running total so nothing is silently dropped -- invariant 6 requires the discard be loud, not frequent). NOT DONE IN ACK-6 because internal/ack/store.go was staged by a concurrently-running agent (ACK-9) and editing it would have swept another agent's work into ACK-6's commit. PROOF: a test that drives N conflicting settles and asserts the log line count is bounded, RED before the fix.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [ACK-6](../ACK-6--d3c50d33/task.md) — ACK-6: Recipient delivery acknowledgement boundary (done)
- [ACK-9](../ACK-9--08f9987f/task.md) — ACK-9: Sender CLI/API acknowledgement status and observability (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
