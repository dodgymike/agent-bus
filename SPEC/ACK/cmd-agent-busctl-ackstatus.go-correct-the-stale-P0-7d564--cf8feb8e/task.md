# cmd/agent-busctl/ackstatus.go: correct the stale "P0 7d564118 is CLOSED" comment

| Field | Value |
| --- | --- |
| Public id | `cf8feb8e-b845-43d7-bd5f-7b5e9074e4d2` |
| Key | _(null in the export)_ |
| Epic | [ACK](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-22T06:19:53.823012+00:00 |
| Updated | 2026-08-22T06:19:53.823012+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/proof-check.sh "! grep -n 'is CLOSED' cmd/agent-busctl/ackstatus.go"
```

## Description

`cmd/agent-busctl/ackstatus.go:60` states "P0 7d564118 is CLOSED." That is false both as a statement about the Spec Server task record (7d564118 was reworked 2026-08-22 and remains todo/P0 -- see its journal) and as a statement about the underlying behaviour: ACK-5 made a relayed message ackable on the receiving bus (that part is genuinely fixed), but `internal/hub/hub.go:2197` `recordAcceptance` still writes no delivery-lifecycle row for a relayed OR broadcast message (`if h.acks == nil || relayed || broadcast { return }`), so ack authorisation for those cases still depends on message retention outliving `ack.Retention` (24h) -- an unclosed durability gap, not a closed task.

Fix: reword ackstatus.go:40 and :60 (and any other reference to 7d564118 in that file) to say ACK-5 made relayed messages ackable, while the destination/broadcast delivery-lifecycle-row question (7d564118, still P0/todo) stays open. Do not just delete the word CLOSED -- state accurately what is and is not done, per the same standard 7d564118's own rework used.

FILED BY spec-keeper ON COORDINATOR DIRECTION 2026-08-22, deliberately NOT edited directly because cmd/agent-busctl/ackstatus.go is one of 22 paths a concurrently-running integrator is committing (an unrelated ACK-5 wave); this task lands the correction afterward.

Proof sketch: `! grep -n 'is CLOSED' cmd/agent-busctl/ackstatus.go` combined with a positive check that the replacement text distinguishes ackability (fixed) from the lifecycle-row/retention-binding question (open) -- pin the specific line, per CLAUDE.md's doc-proof standard, not an incidental match elsewhere in the file.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [ACK-12-FU-DESTINATION-ROW](../ACK-12-FU-DESTINATION-ROW--7d564118/task.md) — ACK-12-FU-DESTINATION-ROW: a relayed message gets no ack row on the DESTINATION bus, so i… (done)
- [ACK-5](../ACK-5--5991ee1a/task.md) — ACK-5: Multi-hop relay ACK/NACK propagation and correlation (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
