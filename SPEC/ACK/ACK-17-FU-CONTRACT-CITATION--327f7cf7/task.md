# ACK-17-FU-CONTRACT-CITATION: pin CONTRACTS-HTTP.md per-principal parked-wait cap claim to its test

| Field | Value |
| --- | --- |
| Public id | `327f7cf7-12d5-4ab8-8437-ca144935bfd3` |
| Key | _(null in the export)_ |
| Epic | [ACK](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-21T15:05:15.560781+00:00 |
| Updated | 2026-08-21T15:05:15.560781+00:00 |
| Completed | — |

## Description

OPTIONAL / NICE-TO-HAVE. NOT owed under CLAUDE.md section 9 -- no route, param, env var or
record-type moved as part of ACK-17, so no CONTRACTS-*.md update was strictly required. Filed
as a follow-up rather than made inline because it edits an existing line in a shared file while
sibling agents were live.

CONTRACTS-HTTP.md:456 (the maxParkedAckStatusPerAgent row) already claims the parked-wait cap is
"keyed on the authenticated principal" and that "self-starvation between two connections of the
SAME agent is possible and accepted". That claim is now ENFORCED by two tests added under
ACK-17: TestAckStatusParkedWaitCapIsPerPrincipalNotPerSession and
TestAckStatusParkedWaitCapIsPerAgentIdNotPerEnrolmentName.

House style in CONTRACTS-HTTP.md already cites pinning tests by name (see lines 151 and 829).
Suggested minimal additive wording for the maxParkedAckStatusPerAgent row: append
"TestAckStatusParkedWaitCapIsPerPrincipalNotPerSession pins this." (Consider also citing the
second test for the unqualified-name-vs-agent-id distinction, at the documentation owner's
discretion.)

Filed 2026-08-21 by spec-keeper at the direction of feature-runner on ACK-17.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [ACK-17](../ACK-17--d4a2d828/task.md) — ACK-17: four keying mutations still leave internal/httpapi green -- session-keyed is the… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
