# ACK-5-FU-STALEWATCHCOMMENT: internal/hub/ack.go still says agent-busctl watch does not expose the origin id

| Field | Value |
| --- | --- |
| Public id | `b5ffc730-6b52-450b-a4f3-83a2d471b820` |
| Key | _(null in the export)_ |
| Epic | [ACK](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | ack |
| Section | backlog |
| Tags | — |
| Created | 2026-08-21T22:24:07.576635+00:00 |
| Updated | 2026-08-22T06:31:24.184699+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/doc-check.sh -- grep pinning the corrected sentence at internal/hub/ack.go, observed RED before the fix.
```

## Description

Found by the security gate while auditing ACK-12-FU-WATCH-CORRELATION-KEY, 2026-08-21, in ACK-5s own (then-uncommitted) file. `internal/hub/ack.go` around lines 717-719 says "`agent-busctl watch` prints the LOCAL message id and does not expose the origin id at all".

VERIFIED 2026-08-22 (spec-keeper, post ACK-5 landing at commit 493450f): the premise that this comment is ALREADY stale is NOT TRUE. `cmd/agent-busctl/watch.go`'s watchRecord (~line 317) still carries only `MessageID string `json:"message_id"`` and no origin/correlation id field -- the comment remains accurate as of ACK-5.

The comment becomes stale ONLY once ACK-12-FU-WATCH-CORRELATION-KEY (f423959c) lands and adds a correlation_key field to the watch stream sourced from store.Message.OriginID(). Filed as a follow_up of f423959c (see relations) so it surfaces at the right time -- do NOT action this until that task lands.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [ACK-12-FU-WATCH-CORRELATION-KEY](../ACK-12-FU-WATCH-CORRELATION-KEY--f423959c/task.md) — ACK-12-FU-WATCH-CORRELATION-KEY: \`watch\` never exposes the origin message id, so a recipi… (done)
- [ACK-5](../ACK-5--5991ee1a/task.md) — ACK-5: Multi-hop relay ACK/NACK propagation and correlation (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
