# ACK-6-FU-CLI: agent-busctl ack, and the canonical ACK bytes it must sign

| Field | Value |
| --- | --- |
| Public id | `836c9ff8-fc58-4989-95cb-7e2086f0eae4` |
| Key | _(null in the export)_ |
| Epic | [ACK](../epic.md) |
| Status | in_progress |
| Priority | P1 |
| Component | cli |
| Section | backlog |
| Tags | — |
| Created | 2026-08-16T16:01:51.307970+00:00 |
| Updated | 2026-08-21T10:08:33.076053+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/proof-check.sh 'go test -race ./internal/signing ./cmd/agent-busctl'
```

## Status note

Part (a), the canonical ACK bytes (internal/signing/ack.go, AckFormatVersion=3, AckContext=agent-bus/recipient-ack/3), is code-complete and awaiting commit at base 591355f; reviewer re-verified RESOLVED, security PASS. Part (b), the agent-busctl ack subcommand + AGENT_PROTOCOL.md entry + CONTRACTS-CLI.md row, is NOT started and invariant 7 remains unmet until it lands.

## Description

INVARIANT 7 GAP, raised as a BLOCKER by both ACK-6 gates. POST /v1/ack shipped in ACK-6 with NO agent-busctl subcommand and NO AGENT_PROTOCOL.md entry, so today the only way to use it is the hand-written HTTP call invariant 7 forbids. TWO reasons it was not written inside ACK-6, and the second is the one that matters:\n\n1. BOUNDARY. cmd/agent-busctl/ and client/ were assigned to ACK-9, which was running concurrently and had cmd/agent-busctl/ackstatus.go and client/ackstatus.go in the tree. ACK-CONTRACT.md S13.4 assigns `agent-busctl ack <message-id> [--refuse <class>]` to ACK-9 alongside `ack-status` so one agent workflow ships as one CLI surface.\n\n2. THE COMMAND CANNOT BE WRITTEN CORRECTLY YET. relay.ValidateAckAttestation REQUIRES a 64-byte detached signature on every recipient-sourced outcome, and internal/signing has NO canonical-ACK-bytes function -- ACK-CONTRACT.md S6.3 mandates one (produced by internal/signing, reusing S8.2's length-prefixed framing) and it does not exist. A CLI written today would have to sign SOMETHING, and whatever it chose would become the de-facto encoding that S6.3's framing task then has to live with. Invariant 9 forbids inventing a construction, and this is that in miniature: an ENCODING nobody specified, frozen by accident.\n\nSO THIS TASK IS TWO THINGS AND THE ORDER IS FIXED: (a) define the canonical ACK bytes in internal/signing, reusing the existing length-prefixed framing, fixed field order, big-endian fixed-width integers, every variable-length field length-prefixed -- an ENCODING ONLY, no new primitive, no reuse of the WAL MAC key; then (b) ship `agent-busctl ack` + `--refuse <class>` + `--json`, its AGENT_PROTOCOL.md entry and its CONTRACTS-CLI.md row, reusing the existing stable exit codes (unknown -> ExitEmpty 8, a negative terminal -> ExitRejected 7) and minting no new code. Until (a) lands, every signature on this plane is 64 bytes over nothing anyone agreed on -- shape-checked, never verified, and end-to-end unverifiable anyway (S16 Q1).

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [ACK-6](../ACK-6--d3c50d33/task.md) — ACK-6: Recipient delivery acknowledgement boundary (done)
- [ACK-9](../ACK-9--08f9987f/task.md) — ACK-9: Sender CLI/API acknowledgement status and observability (done)

## Referenced by other tasks (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [ACK-15](../ACK-15--a63b133d/task.md) — ACK-15: POST /v1/ack has no CLI subcommand -- until it does, no row can ever reach delive… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
