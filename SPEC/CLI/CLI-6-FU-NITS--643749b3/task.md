# CLI-6-FU-NITS: three non-blocking nits in \`agent-bus log\` raised by the reviewer and security gates

| Field | Value |
| --- | --- |
| Public id | `643749b3-be92-4d30-93b9-84d5ff5d6773` |
| Key | CLI-6-FU-NITS |
| Epic | [CLI](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | cli |
| Section | backlog |
| Tags | — |
| Created | 2026-08-14T13:07:59.608675+00:00 |
| Updated | 2026-08-14T13:07:59.608675+00:00 |
| Completed | — |

## Proof command

```sh
go test -race -run 'TestCLILog' ./cmd/agent-bus/...
```

## Description

Three non-blocking nits raised during CLI-6 review/security gates:

1. Overstated comment. cmd/agent-bus/auditlog.go, the readAuditTrail comment near line 479, claims a lone bus.lock makes the operators first server start refuse to boot. internal/dirlock is flock-based and Release deliberately never unlinks, so a stale lock file does NOT block Acquire. The pre-lock ordering it justifies is still correct; only the stated reason is wrong. Fix the comment, do not change the ordering.

2. Exit code for a non-directory -data-dir. Pointing -data-dir at a regular file returns exit 4 ("no trail") rather than exit 2 (usage). Defensible, but decide and document.

3. Missing test. There is no test for checkAuditFormatVersions os.Open-EACCES branch (only the directory-level case in TestCLILogUnreadableTrailIsNotReportedAsMissing). That gap is why a documentation/code drift on exit 5 was caught by a human reviewer rather than mechanically.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [CLI-6](../CLI-6--47001cb4/task.md) — CLI-6: log -- read the append-only audit log (metadata only; also absorbs the WAL-dumper… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
