# AUTH-5-FU-REVOCATION: agent-level revocation-recovery crash-injection test, blocked on AUTH-4

| Field | Value |
| --- | --- |
| Public id | `fa579717-e73e-41dd-a3d5-884663f092b0` |
| Key | _(null in the export)_ |
| Epic | [AUTH](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | auth |
| Section | backlog |
| Tags | — |
| Created | 2026-08-22T22:56:27.591664+00:00 |
| Updated | 2026-08-22T22:56:27.591664+00:00 |
| Completed | — |

## Proof command

```sh
go test -race -run TestRevocationCrashRecovery ./internal/auth
```

## Description

AUTH-5 (2b0791d4) covers the enrol/commit crash-recovery iff and the token-stays-rejected clause via invariant-3 session non-persistence, but NOT a genuine revoke-then-crash-then-recover test at the AGENT level: revoke an agent, crash, restart, and assert the agent token stays rejected because the REVOCATION itself survived the crash durably. That test cannot be written yet because WALRoster has no durable remove path -- AUTH-4 (a853261d, POST /v1/leave) has not shipped. Once AUTH-4 lands, add a crash-injection subtest (same pattern as authcrashrecovery_test.go: real SIGKILL at points around the revocation write, restart from WAL alone, assert the revoked agent token is rejected and cannot be re-established without re-enrolling) covering invariants 4/5/6 for the revocation write path specifically, not just the enrol path.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


- **blocked by** [AUTH-4](../AUTH-4--a853261d/task.md)

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [AUTH-4](../AUTH-4--a853261d/task.md) — AUTH-4: POST /v1/leave -- leave / revocation (done)
- [AUTH-5](../AUTH-5--2b0791d4/task.md) — AUTH-5: Auth crash/recovery test (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
