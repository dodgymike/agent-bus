# AUTH-8-FU-POSTURE-DOCS: document the composed anonymous-surface abuse posture (what each control charges, fail-open-when-disabled, distributed-flood residual)

| Field | Value |
| --- | --- |
| Public id | `46ede035-093f-448d-8f8e-bf4bcf18e5db` |
| Key | _(null in the export)_ |
| Epic | [AUTH](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-22T23:04:32.169800+00:00 |
| Updated | 2026-08-22T23:04:32.169800+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/doc-check.sh section CONTRACTS-HTTP.md 'anonymous credential surface'
```

## Description

From AUTH-8 deep dive (gap G10 + landmines). No code. Add one section to CONTRACTS-HTTP.md (or a docs/ note) stating the composed model of the anonymous credential surface (/v1/enroll, /v1/session/begin, /v1/session/complete): (1) what each control charges -- rate limit charges the SOURCE, invite gate charges the CREDENTIAL, roster/session/idempotency caps are the global memory backstops; (2) the per-source rate limiter FAILS OPEN when disabled (-auth-rate-burst 0), and disabling it to work around shared-source starvation removes the only control charging a /v1/session/begin flood; (3) a DISTRIBUTED flood from many sources each under its own 5/s budget still fills the global session table (D) -- this is a KNOWN, ACCEPTED residual needing network-layer defence, not a bug (session.go:242-255, 426-438); (4) the shared-source (Docker bridge / NAT / tunnel) caveat, cross-referencing AUTH-8-FU-RATELIMIT-SHAREDSRC. Docs-only: security gate SKIPPED unless it touches a control-plane/guard file. proof_cmd sketch pins the new heading.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [AUTH-8](../AUTH-8--b65948b7/task.md) — AUTH-8: DEEP DIVE — the balance between usability and security / abuse protection (done)
- [AUTH-8-FU-RATELIMIT-SHAREDSRC](../AUTH-8-FU-RATELIMIT-SHAREDSRC--fe0245a3/task.md) — AUTH-8-FU-RATELIMIT-SHAREDSRC: co-located honest agents share one rate-limit bucket behin… (todo)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
