# Rate limiter GC sweep (maybeGCLocked) is O(n) under the mutex once per 60s -- revisit only at large untrusted-IPv6-range scale

| Field | Value |
| --- | --- |
| Public id | `855dd855-2017-43eb-8d22-d1127387a8f3` |
| Key | _(null in the export)_ |
| Epic | [AUTH](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | auth |
| Section | backlog |
| Tags | — |
| Created | 2026-08-22T22:56:27.791598+00:00 |
| Updated | 2026-08-22T22:56:27.791598+00:00 |
| Completed | — |

## Proof command

```sh
none -- design/scale revisit, not currently actionable
```

## Description

Follow-up from AUTH-1-FU-RATELIMIT (42670f8b, committed da2d58d). internal/httpapi/ratelimit.go maybeGCLocked walks the whole per-source bucket map under rl.mu once every 60s to drop fully-refilled buckets, bounding memory. Both the reviewer and security gates flagged this as non-blocking at current scale (bounded by real distinct source IPs). Worth revisiting only if the bus is ever exposed to a large untrusted IPv6 range, where distinct-source cardinality can grow far faster than a typical IPv4-facing deployment and the O(n) sweep under a single mutex could become a contention point. No action needed now.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [AUTH-1-FU-RATELIMIT](../AUTH-1-FU-RATELIMIT--42670f8b/task.md) — AUTH-1-FU-RATELIMIT: per-source rate limiting on the three unauthenticated auth routes (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
