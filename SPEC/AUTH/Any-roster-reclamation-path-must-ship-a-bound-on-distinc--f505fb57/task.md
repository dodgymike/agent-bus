# Any roster-reclamation path must ship a bound on distinct agent names in the SAME change -- internal/ids has none and borrows its bound from the roster cap's monotonicity

| Field | Value |
| --- | --- |
| Public id | `f505fb57-25ab-46e1-a7a1-2ca5787529ab` |
| Key | _(null in the export)_ |
| Epic | [AUTH](../epic.md) |
| Status | todo |
| Priority | P1 |
| Component | ids |
| Section | backlog |
| Tags | — |
| Created | 2026-08-14T21:01:19.970941+00:00 |
| Updated | 2026-08-14T21:01:19.970941+00:00 |
| Completed | — |

## Description

internal/ids explicitly has NO cap on distinct names and says the bound is admission control's job -- agentmint.go:163-170 ("bounding how many distinct names an unauthenticated caller may create is AUTH-1's job... This type cannot bound it") and suffixstore.go:221-222 ("Bounding that is admission control's job (the roster cap), and this type cannot do it: it may not forget a name"). The ONLY thing keeping the per-name suffix-floor file finite today is that the roster never gives a slot back. So adding eviction, a TTL, or /v1/leave lets enrol/leave churn with fresh names grow N without limit, and every new name rewrites the whole file -- an unbounded O(N^2) write amplifier on an unauthenticated route (suffixstore.go:181-185 predicts exactly this). Bounded worst case TODAY is benign because N caps at 4096: 278,628-byte file, ~545 MiB lifetime write amplification, 8,192 fsyncs. Must be linked as a BLOCKING dependency of AUTH-ROSTER-RECLAIM (b418638c) and AUTH-4 (a853261d) rather than discovered afterwards.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [AUTH-1](../AUTH-1--54fa94c0/task.md) — AUTH-1: POST /v1/enroll -- signed credential issuance (done)
- [AUTH-4](../AUTH-4--a853261d/task.md) — AUTH-4: POST /v1/leave -- leave / revocation (todo)
- [AUTH-ROSTER-RECLAIM](../AUTH-ROSTER-RECLAIM--b418638c/task.md) — AUTH-ROSTER-RECLAIM: operator-side "agent-bus roster remove &lt;id&gt;" escape hatch -- filesys… (todo)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
