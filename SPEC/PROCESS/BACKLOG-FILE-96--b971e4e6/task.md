# BACKLOG-FILE-96: file every epic-less task into an epic, or say honestly why not

| Field | Value |
| --- | --- |
| Public id | `b971e4e6-9f1e-4475-bf15-f88a672a9e61` |
| Key | BACKLOG-FILE-96 |
| Epic | [PROCESS](../epic.md) |
| Status | done |
| Priority | P2 |
| Component | process |
| Section | backlog |
| Tags | — |
| Created | 2026-08-08T10:32:20.694670+00:00 |
| Updated | 2026-08-08T10:33:24.477120+00:00 |
| Completed | 2026-08-08T10:33:24.477104+00:00 |

## Proof command

```sh
bash scripts/spec-cloud.sh -s "/api/v1/projects/agent-bus/export?format=json" | python3 -c 'import json,sys; d=json.load(sys.stdin); eks={e["key"] for e in d["epics"]}; u=[x for x in d["tasks"] if not x.get("epic_key")]; assert {"PROCESS","TOOLING"}<=eks, eks; assert len(u)<=2 and all(x["status"]=="cancelled" for x in u), u; print("BACKLOG_FILED_OK unfiled=%d"%len(u))'
```

## Description

One-off backlog-organisation pass, 2026-08-08. 96 of 303 tasks carried no epic_key, so half the backlog was invisible to anyone scanning epics or running claim-next with an ?epic= filter.

RULE APPLIED: epics are DISPATCH UNITS, not taxonomy. Where theme and file-ownership boundary disagreed, ownership won -- an epic that mixes internal/wal with client/ cannot be handed to one agent.

TWO NEW EPICS, keys claimed via reservation namespace 'epic-key' (values 1 and 2), never eyeballed:
  PROCESS -- how agents coordinate + backlog integrity (7 tasks)
  TOOLING -- the repo's own scripts/, .gitignore, dev env (7 tasks)

A THIRD PROPOSED EPIC WAS REJECTED: a cross-cutting SECURITY epic. component=security was the largest group among the unfiled (18), but on inspection it records WHO FOUND the task (the security gate), not what code it touches: 8 were plainly MTLS, 2 INVITE, 2 CLI/AGENTIF, 1 IDEM, 1 DUR, 2 CORE, 1 a superseded epic definition, and exactly ONE was genuinely cross-cutting. A SECURITY epic would have taken 8 tasks away from the agent who owns the TLS code. See the kind=report note for the full reasoning.

LEFT UNFILED, DELIBERATELY: 2 cancelled test artefacts (ZZ-LOCKTEST, keypatch-probe). Both were already cancelled before this pass -- verified, not acted on.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [ZZ-LOCKTEST](../../UNASSIGNED/ZZ-LOCKTEST--e091e451/task.md) — ZZ-LOCKTEST: verify If-Match CAS (cancelled)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
