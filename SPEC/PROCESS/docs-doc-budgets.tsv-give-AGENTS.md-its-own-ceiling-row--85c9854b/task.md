# docs/doc-budgets.tsv: give AGENTS.md its own ceiling row

| Field | Value |
| --- | --- |
| Public id | `85c9854b-6c34-470f-bffe-3eed1116f2b0` |
| Key | _(null in the export)_ |
| Epic | [PROCESS](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | process |
| Section | backlog |
| Tags | — |
| Created | 2026-08-22T09:04:46.955673+00:00 |
| Updated | 2026-08-22T09:04:46.955673+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/proof-check.sh 'bash scripts/doc-check.sh budget && grep -c "^AGENTS.md" docs/doc-budgets.tsv'
```

## Description

`AGENTS.md` is injected into every sub-agent spawn for runtimes that read AGENTS.md rather than
CLAUDE.md, and it is byte-identical to `CLAUDE.md` (measured 2026-08-22: both 28776 bytes in the
worktree, both 28213 at HEAD). It has **NO row in `docs/doc-budgets.tsv`** (measured: 46 rows,
`CLAUDE.md` at line 44 with ceiling 28781; `grep -c "^AGENTS.md" docs/doc-budgets.tsv` prints `0`
and exits 1).

It is budgeted only TRANSITIVELY, via the `cmp CLAUDE.md AGENTS.md` that lives in another task's
proof. Weaken or drop that `cmp` and `AGENTS.md` becomes unbudgeted while still being injected into
every spawn. That is a check whose enforcement depends on another check nobody is watching -- the
control-plane bug in miniature (`PITFALLS.md` section 8).

This is filed SEPARATELY from e4e31233 (the CLAUDE.md tiered-rule rewrite) precisely because that
task can land with the `cmp` intact and this row still missing. The two are independent.

Scope:
- Add an `AGENTS.md` row to `docs/doc-budgets.tsv`, using the same ratchet discipline applied to
  `CLAUDE.md`: the ceiling is set at the file's size AT THE MOMENT THE ROW LANDS, not a round number
  and not a generous headroom allowance.
- Add the explanatory paragraph the file's header convention expects (say WHY AGENTS.md is
  budgeted: it is injected per spawn, and it is the CLAUDE.md twin).
- Do not change the `cmp CLAUDE.md AGENTS.md` check in the other task -- this row is additional
  defence, not a replacement.

Proof (VERIFY THE EXACT FORM BEFORE STORING IT, and confirm it is RED before the row exists):
`bash scripts/proof-check.sh 'bash scripts/doc-check.sh budget && grep -c "^AGENTS.md" docs/doc-budgets.tsv'`
Judge by OUTPUT, not exit status: `grep -c` prints `0` and exits 1 on no match (the RED state
measured 2026-08-22), and prints `1` exit 0 once the row lands. Quote the RED output in the
kind=report note -- a proof never observed failing is not evidence (PITFALLS.md section 2).

RELATES to e4e31233-cabe-4af4-986b-f28c84347214.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


- **relates to** [e4e31233-cabe-4af4-986b-f28c84347214](../CLAUDE.md-replace-the-flat-mandated-chain-rule-with-the--e4e31233/task.md)

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [e4e31233-cabe-4af4-986b-f28c84347214](../CLAUDE.md-replace-the-flat-mandated-chain-rule-with-the--e4e31233/task.md) — CLAUDE.md: replace the flat mandated-chain rule with the tiered one-liner, byte-neutral,… (todo)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
