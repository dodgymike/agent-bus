# change-tier.sh: concurrency-primitive signal

| Field | Value |
| --- | --- |
| Public id | `1aa8c031-6e46-49a5-b572-70d53a1d64ba` |
| Key | _(null in the export)_ |
| Epic | [PROCESS](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | process |
| Section | backlog |
| Tags | — |
| Created | 2026-08-22T08:40:03.305507+00:00 |
| Updated | 2026-08-22T08:40:03.305507+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/proof-check.sh 'bash scripts/change-tier_test.sh'
```

## Description

Signal 4. Fire on added `go `/`go\t` (goroutine launch), `sync.`, `chan`, `select {`/`select{` in non-test .go files -> -race mandatory + reliability-reviewer.

Strip comments and string literals before matching. Without that, `go ` matches `go test`, `go build`, `golang` and prose in doc comments -- this package is heavily commented and the signal would fire almost everywhere, which per T-01's noise caveat is a failure mode, not conservatism. Match `chan<-`, `<-chan` and `chan(` as well as `chan `.

Proof detail: fixtures for a real `go func`; sync.Mutex; `chan<-`; and negative controls (`// go to the next record`, a string containing "go build"). RED first.

BLOCKED BY T-03.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


- **blocked by** [b2567ffd-190d-4aff-8cc2-f6a2eb2d613e](../scripts-change-tier.sh-diff-basis-contract-output-format--b2567ffd/task.md)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
