# DECISIONS.md: record the tiered review chain and the raise-only asymmetry

| Field | Value |
| --- | --- |
| Public id | `51e0993f-76e0-40fd-b6a0-cd7d83d83548` |
| Key | _(null in the export)_ |
| Epic | [PROCESS](../epic.md) |
| Status | todo |
| Priority | P1 |
| Component | process |
| Section | backlog |
| Tags | — |
| Created | 2026-08-22T08:40:01.705543+00:00 |
| Updated | 2026-08-22T08:40:01.705543+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/proof-check.sh 'bash scripts/doc-check.sh section DECISIONS.md "Tiered review chain" "raise-only" "mechanical floor"'
```

## Description

Append a dated entry to DECISIONS.md recording the decision, its evidence and what was rejected. Must capture:

- The measurement that motivated it: 1000 notes sampled; 60 security verdicts across 35 tasks; 27% demanded changes; 51% of tasks needed 2+ passes -- so security is load-bearing and this routes effort rather than removing it.
- That size does not predict risk and invariant PLANE does: ACK-5 auth/relay/egress -> blocking finding; scripts/doc-check.sh -> injectable TMPDIR + sed option-injection; IDEM -> clean.
- That INVARIANTS.md is reused as the risk taxonomy rather than a parallel one being invented.
- The raise-only asymmetry and its rationale.
- The explicitly REJECTED alternative -- collapsing security into a single commit-time pass like integrator -- with ACK-5's architectural fix as the evidence against it.

DECISIONS.md is shared append-only: single owner, one dated section, never edit existing lines.

Proof note: the heading wording may differ from the stored proof_cmd. Set the proof to whatever heading is actually written, and confirm it is RED before the entry lands.

BLOCKED BY T-01.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


- **blocked by** [4d990ef4-23ee-4971-ab00-84eb5ec137ae](../Write-docs-CHANGE-TIERS.md-the-normative-tier-and-signal--4d990ef4/task.md)

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [ACK-5](../../ACK/ACK-5--5991ee1a/task.md) — ACK-5: Multi-hop relay ACK/NACK propagation and correlation (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
