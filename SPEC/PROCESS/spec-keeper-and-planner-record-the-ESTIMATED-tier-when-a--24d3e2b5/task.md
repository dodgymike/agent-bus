# spec-keeper and planner record the ESTIMATED tier when a task is created

| Field | Value |
| --- | --- |
| Public id | `24d3e2b5-0d12-484b-8430-6f421a10c275` |
| Key | _(null in the export)_ |
| Epic | [PROCESS](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | process |
| Section | backlog |
| Tags | — |
| Created | 2026-08-22T08:40:04.941862+00:00 |
| Updated | 2026-08-22T08:40:04.941862+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/proof-check.sh 'bash scripts/doc-check.sh section .claude/agents/spec-keeper.md "Estimated change tier" "estimated" "kind=tier"'
```

## Description

The design has two phases, ESTIMATED and MEASURED, but the original decomposition contains no task that makes the estimate happen -- without it the measured number has nothing to compare against and drift detection (T-17) is impossible.

Update .claude/agents/spec-keeper.md and .claude/agents/planner.md: at task creation, declare the invariant planes the task is expected to touch and record `kind=tier; estimated=T<n>` on the new task. Cheap and explicitly allowed to be wrong -- being wrong is the signal T-17 reads.

Proof note: adjust the heading in the proof to whatever is actually written, and confirm the proof is RED before the edit lands.

BLOCKED BY T-01. BLOCKS T-17.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
