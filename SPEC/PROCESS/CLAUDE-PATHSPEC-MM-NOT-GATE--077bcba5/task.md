# CLAUDE.md: MM is not the gate -- a clean M can still hide another agent's worktree hunks

| Field | Value |
| --- | --- |
| Public id | `077bcba5-1e3f-4687-baf3-0efe9620dc7f` |
| Key | CLAUDE-PATHSPEC-MM-NOT-GATE |
| Epic | [PROCESS](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-14T16:25:17.371644+00:00 |
| Updated | 2026-08-14T16:25:17.371644+00:00 |
| Completed | — |

## Proof command

```sh
grep -qF 'catches only ONE direction' CLAUDE.md
```

## Description

Filed retroactively 2026-08-14 by spec-keeper -- the coordinator dispatched this work without a task record first (their own process error), which left the implementing agent unable to post its required notes. Filed after the fact so the journal has a home.

THE WORK: a five-line addition to CLAUDE.md's pathspec-commit-trap section (now committed at d71d5f5), making the hunk-level worktree diff UNCONDITIONAL rather than triggered by an MM status.

THE SHARPER DIAGNOSIS (corrects the coordinator's own first framing): the coordinator initially described the near-miss incident as having NO warning in CLAUDE.md. It had one -- the prior text already said 'check git status --porcelain for an MM, and diff the worktree.' The REAL defect was narrower: MM was framed as the TRIGGER that sends an agent to the diff, so a clean ' M' (index clean, worktree dirty -- the mirror case) reads as clearing the agent to commit, when it should not. The implementing agent identified this narrower framing defect and wrote the fix against it, not against the coordinator's original (wrong) 'no warning' framing.

THE NEAR-MISS IT CITES: client/client.go at ' M' with a clean index during the 0d31d2f documentation commit, carrying one in-scope doc-comment fix (RELAY-13-FU-DOCS's fifth site) plus endpointWith and resolvePinsWith -- two functions from INVITE-CLIENT's live, ungated task, in the SAME file. An integrator caught it by diffing the worktree rather than trusting the clean index or the (misleading, on the old framing) absence of an MM flag, and held the file back from that commit rather than ship unreviewed code under a docs title.

TWO THINGS WORTH NOTING FOR THE RECORD: the implementing agent refused to record a reviewer gate it had not run (no Task tool available to it), and refused to post its notes to an unrelated in-progress task rather than pollute another journal -- both were the right call, and the second was only necessary because no task existed yet for this work. This task exists so that is no longer necessary next time.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [INVITE-CLIENT](../../INVITE/INVITE-CLIENT--4123e25d/task.md) — INVITE-CLIENT: the Go client/CLI redeems an invite at enrol (+ AGENT_PROTOCOL.md entry) -… (done)
- [RELAY-13-FU-DOCS](../../RELAY/RELAY-13-FU-DOCS--7f3a4b80/task.md) — RELAY-13-FU-DOCS: three docs/comments assert the opposite of shipped RELAY-13 behaviour -… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
