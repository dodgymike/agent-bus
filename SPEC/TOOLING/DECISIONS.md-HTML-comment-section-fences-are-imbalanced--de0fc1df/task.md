# DECISIONS.md HTML-comment section fences are imbalanced (6 BEGIN / 8 END) -- introduced by e7a3c49, not inherited

| Field | Value |
| --- | --- |
| Public id | `de0fc1df-a948-4b44-95a4-4b9d01cab267` |
| Key | _(null in the export)_ |
| Epic | [TOOLING](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-14T16:13:50.330971+00:00 |
| Updated | 2026-08-14T16:13:50.330971+00:00 |
| Completed | — |

## Proof command

```sh
test "$(grep -c '===== BEGIN' DECISIONS.md)" -eq "$(grep -c '===== END' DECISIONS.md)"
```

## Description

Filed 2026-08-14. Cosmetic TODAY, confirmed by grep: zero .go/.sh files reference the '===== BEGIN'/'===== END' marker strings anywhere in the repo (independently re-verified before filing, not just relayed).

THE FACT, VERIFIED AGAINST BOTH COMMITS DIRECTLY (git show <sha>:DECISIONS.md | grep -c): commit ed77bba was BALANCED -- exactly 5 '===== BEGIN' and 5 '===== END' markers. The next commit, e7a3c49, took it to 6 BEGIN / 8 END -- introduced there, not inherited from an earlier imbalance. Of the four 2026-08-14 sections landed across ed77bba/e7a3c49: MTLS-CLIENTAUTH (4480/4568) and RELAY-24-BLOCKER-HUBINGEST (4814/4957) are correctly balanced (BEGIN+END each); CLI-11 (4716) and INVITE-GATE (4812) each have an END fence with NO BEGIN; RELAY-45 has NEITHER a BEGIN NOR an END fence for its section at all.

SAID PLAINLY, PER THE COORDINATOR'S OWN INSTRUCTION: this was reported as pre-existing TWICE before being corrected, and both times that was wrong. The integrator established the truth by diffing HEAD (git show <sha>:DECISIONS.md) rather than accepting either report at face value -- the correct method here was checking the artifact directly across the two commits, not trusting a claim about it, including this task's own filer's first two attempts.

WHY IT IS NOT ZERO-RISK DESPITE BEING COSMETIC TODAY: the RELAY-24-BLOCKER-HUBINGEST agent relied on its OWN section's fences to make that section mechanically extractable when the coordinator needed it held out of a commit separately from the rest of the working tree -- and that extraction worked BECAUSE its fences were balanced (a script or a human can safely `sed -n '/BEGIN X/,/END X/p'` a balanced pair; an unmatched END with no BEGIN, or a section with neither, cannot be extracted that way at all, or worse, silently grabs the wrong span). A future extractor that assumes every dated section is a well-formed BEGIN/END pair will mis-cut exactly the three sections named above (CLI-11, INVITE-GATE, RELAY-45) -- CLI-11 and INVITE-GATE by grabbing content that doesn't belong (no BEGIN to anchor the start), RELAY-45 by finding nothing to extract at all despite the section existing in the file.

SCOPE: add the four missing fence comments (BEGIN for CLI-11, BEGIN for INVITE-GATE, BEGIN+END for RELAY-45) matching the existing style of the balanced sections (MTLS-CLIENTAUTH, RELAY-24-BLOCKER-HUBINGEST) exactly. DECISIONS.md is append-only and multi-claimant this session -- check `git status --porcelain -- DECISIONS.md` and diff the WORKTREE before any edit or commit, per CLAUDE.md's pathspec-commit trap. This is a pure insertion of HTML comments around EXISTING text -- no prose content changes.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [CLI-11](../../UNASSIGNED/CLI-11--bf966c07/task.md) — CLI-11: export the bus signing public key from the operator CLI (done)
- [INVITE-GATE](../../INVITE/INVITE-GATE--05a5216d/task.md) — INVITE-GATE: POST /v1/enroll REQUIRES a valid invite and fails closed; invite consumption… (done)
- [MTLS-CLIENTAUTH](../../MTLS/MTLS-CLIENTAUTH--cc9558a8/task.md) — MTLS-CLIENTAUTH: request a client certificate on every connection WITHOUT a CA -- tls.Req… (done)
- [RELAY-24-BLOCKER-HUBINGEST](../../RELAY/RELAY-24-BLOCKER-HUBINGEST--9ee98866/task.md) — RELAY-24-BLOCKER-HUBINGEST: internal/hub exported relay-ingest entry point -- foreign sen… (done)
- [RELAY-45](../../RELAY/RELAY-45--4be32336/task.md) — RELAY-45: Bind inbound peer TLS certificate to the adjacent bus principal (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
