# TOOLING-1: read-only linter for mechanically broken stored proof_cmd values

| Field | Value |
| --- | --- |
| Public id | `eeb4109b-49cb-48bd-944b-c4efb3003843` |
| Key | TOOLING-1 |
| Epic | [TOOLING](../epic.md) |
| Status | done |
| Priority | P1 |
| Component | tooling |
| Section | backlog |
| Tags | — |
| Created | 2026-08-14T21:29:10.956164+00:00 |
| Updated | 2026-08-14T21:59:00.924570+00:00 |
| Completed | 2026-08-14T21:59:00.924553+00:00 |

## Proof command

```sh
bash scripts/proof-cmd-audit-test.sh
```

## Description

Owner dispatch seq2401. Build a read-only linter that consumes the complete authoritative Spec Server JSON export and ranks mechanically detectable broken stored proof_cmd values. Required findings: nonexistent or vacuous go-test targets; always-zero/success-masking pipeline clauses; conditional constructs where both branches exit successfully. Output must identify the task and reason, be deterministic and severity-ranked, and must never mutate or repair tasks. Explicitly out of scope: judging semantic partial coverage and fixing any reported task. Files: smallest tooling implementation under scripts/ plus focused automated tests and relevant CONTRACTS-AGENT.md documentation; no application behavior changes. Acceptance: a fixture containing one example of every required defect class is reported in rank order, a clean fixture exits cleanly, the complete live export can be scanned read-only, and tests prove no mutation endpoint is invoked. Invariants: read invariant 7 in full before implementation because this is repo tooling consumed by agents; no other product invariant is expected to change.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
