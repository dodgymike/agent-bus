# INVITE-CLIENT-FU-EXIT9: exit code 9 (ExitVersionSkew) is reachable but undocumented, breaking the help-table guard

| Field | Value |
| --- | --- |
| Public id | `9c85d8df-352c-44f7-bb96-7fc6d960484d` |
| Key | INVITE-CLIENT-FU-EXIT9 |
| Epic | [INVITE](../epic.md) |
| Status | done |
| Priority | P2 |
| Component | cli |
| Section | backlog |
| Tags | — |
| Created | 2026-08-14T17:13:11.585243+00:00 |
| Updated | 2026-08-14T19:10:31.587424+00:00 |
| Completed | 2026-08-14T19:10:31.587407+00:00 |

## Proof command

```sh
go test -race -run 'TestHelpExitCodeTablesAgreeWithClientExitCodes|TestEveryVersionSkewCommandDocumentsExitNine' ./cmd/agent-busctl/
```

## Status note

CODE-COMPLETE, GATED, NOT COMPLETE -- HELD FOR DOC ROWS (updated 2026-08-14, supersedes the earlier narrower note). reviewer PASS on the code for both this task and INVITE-CLIENT-FU-PENDINGINVITE together; security PASS-COMPLETED with an independent ADVERSARIAL CROSS-CHECK against its own main finding that came back NOT REFUTED. Held because invariant 7 requires the doc entry in the SAME task: part (a) (the cli_test.go valid-map entry) is in the working tree; part (b) (documenting exit 9 in every help block that can produce it) plus the CONTRACTS-CLI.md/AGENT_PROTOCOL.md rows are outstanding. A documentation agent is on them now; they land in the same commit as INVITE-CLIENT-FU-PENDINGINVITE (7bb6edf0). Do not complete until that commit lands and the doc rows are verified present in it.

## Description

DISCOVERED BY: reviewer/security gate during INVITE-CLIENT (4123e25d-4644-48e0-9ee1-e0a9a273fd8c)

A code comment already references this key, so it MUST exist or the reference dangles (the reviewer found it returns 404).

`cmd/agent-busctl/cli_test.go:287`'s `valid` map -- the closed set `TestHelpExitCodeTablesAgreeWithClientExitCodes` checks documented exit codes against -- omits `client.ExitVersionSkew` (= 9). The constant exists and `client.ExitCode` genuinely returns it for a 404 on a route (the bus is older than this client), so exit 9 is REACHABLE but cannot be documented: adding the row turns that guard RED. Verified during INVITE-CLIENT with the exact failure `cli_test.go:340: enrol documents exit code 9 (...), which is not one of the client.Exit* constants`. The defect is in the GUARD, not in any help table, it predates INVITE-CLIENT, and it affects EVERY subcommand's help. Two halves, in order: (a) add `client.ExitVersionSkew` to the closed set; (b) document exit 9 in every help block that can produce it. `cmd/agent-busctl/enrol.go` carries an explanatory comment naming this key -- remove it when this lands.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [INVITE-CLIENT](../INVITE-CLIENT--4123e25d/task.md) — INVITE-CLIENT: the Go client/CLI redeems an invite at enrol (+ AGENT_PROTOCOL.md entry) -… (done)
- [INVITE-CLIENT-FU-PENDINGINVITE](../INVITE-CLIENT-FU-PENDINGINVITE--7bb6edf0/task.md) — INVITE-CLIENT-FU-PENDINGINVITE: pendingEnrolment does not record the invite id, so a mism… (done)

## Referenced by other tasks (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [8949b094-66ab-4704-8c8d-39dd54981ca1](../../CLI/cmd-agent-busctl-cli_test.go-valid-exit-code-map-omits-c--8949b094/task.md) — cmd/agent-busctl/cli_test.go valid-exit-code map omits client.ExitVersionSkew(9) -- laten… (superseded)
- [CONTRACTS-CLI-1632-STALE](../../DOCS/CONTRACTS-CLI-1632-STALE--ae5011bf/task.md) — CONTRACTS-CLI.md:1632 states a false fact and dangles a reference to a comment the invite… (done)
- [INVITE-CLIENT-FU-EXIT9](../INVITE-CLIENT-FU-EXIT9--970e2a81/task.md) — INVITE-CLIENT-FU-EXIT9: exit code 9 (ExitVersionSkew) is reachable but undocumented, brea… (cancelled)
- [INVITE-CLIENT-FU-PENDINGINVITE](../INVITE-CLIENT-FU-PENDINGINVITE--7bb6edf0/task.md) — INVITE-CLIENT-FU-PENDINGINVITE: pendingEnrolment does not record the invite id, so a mism… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
