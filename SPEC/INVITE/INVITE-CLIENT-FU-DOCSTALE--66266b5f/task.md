# INVITE-CLIENT-FU-DOCSTALE: CONTRACTS-HTTP.md and DECISIONS.md still assert facts INVITE-CLIENT made false

| Field | Value |
| --- | --- |
| Public id | `66266b5f-b8d2-4d93-bffa-e24537b86a11` |
| Key | INVITE-CLIENT-FU-DOCSTALE |
| Epic | [INVITE](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-14T17:13:13.108379+00:00 |
| Updated | 2026-08-14T17:13:13.108379+00:00 |
| Completed | — |

## Proof command

```sh
! grep -qF 'Nor does a CLI exist to exercise the invite half' CONTRACTS-HTTP.md && grep -qF 'invite-file' DECISIONS.md
```

## Description

DISCOVERED BY: reviewer/security gate during INVITE-CLIENT (4123e25d-4644-48e0-9ee1-e0a9a273fd8c)

Two committed documents now assert things that are FALSE since INVITE-CLIENT landed. Both files were outside INVITE-CLIENT's boundary. (a) `CONTRACTS-HTTP.md:642-644` still says "Nor does a CLI exist to exercise the invite half of this route... that gap is task INVITE-CLIENT" -- the CLI exists (`agent-busctl enrol --invite-file`). (b) `DECISIONS.md` §3 (~line 1395) still says "`--invite` is REJECTED locally, not guessed onto the wire" -- the `--invite` flag is REMOVED and `--invite-file` really does put `invite_id`/`invite_secret` on the wire; DECISIONS.md is append-only, so this needs a NEW dated superseding entry, not an edit to the old lines.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [INVITE-CLIENT](../INVITE-CLIENT--4123e25d/task.md) — INVITE-CLIENT: the Go client/CLI redeems an invite at enrol (+ AGENT_PROTOCOL.md entry) -… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
