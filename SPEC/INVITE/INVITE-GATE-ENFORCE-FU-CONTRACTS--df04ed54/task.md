# INVITE-GATE-ENFORCE-FU-CONTRACTS: update CONTRACTS-HTTP.md/CONTRACTS-ONDISK.md to reflect enforced invite gate

| Field | Value |
| --- | --- |
| Public id | `df04ed54-837c-44ef-a969-764cb374ae52` |
| Key | INVITE-GATE-ENFORCE-FU-CONTRACTS |
| Epic | [INVITE](../epic.md) |
| Status | todo |
| Priority | P1 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-14T22:55:28.185304+00:00 |
| Updated | 2026-08-14T22:55:28.185304+00:00 |
| Completed | — |

## Proof command

```sh
grep -n "is NOT gated by this change\|still accepted and still 201" CONTRACTS-HTTP.md CONTRACTS-ONDISK.md; test $? -ne 0 && echo DOCS_UPDATED
```

## Description

INVITE-GATE-ENFORCE (8297d7e2) enforces invite-only enrolment (403 on no/invalid invite), but CONTRACTS-HTTP.md still documents the OLD ungated behaviour at lines ~43, ~91-92, ~464, ~689-690: "Presenting no invite at all is still accepted and still 201 -- enrolment is NOT gated by this change." CONTRACTS-ONDISK.md ~1141-1142 says the same. Fix: update every one of those passages to state the gate is ENFORCED (403 refusal, connection kept, no disconnect per invariant 10), and add a 403 row to the CONTRACTS-HTTP.md enrolment route table for the invite-required refusal (status, body shape, remedy). NOTE: CONTRACTS-HTTP.md is currently held by the live MTLS-CROSSCHECK work (in_progress, owner feature-runner) -- sequence this task AFTER that lands to avoid a lost-update collision on the same file; check git status / task state before editing. This task BLOCKS INVITE-GATE-ENFORCE (8297d7e2) from being marked done per CLAUDE.md step 9 (plane contracts must land in the same increment as the behaviour change) -- reviewer explicitly named this as B4/a must-land-before-done item.

Proof: grep -n the corrected passages in CONTRACTS-HTTP.md and CONTRACTS-ONDISK.md confirming they now assert enforcement, e.g. `grep -n "invite-only\|403" CONTRACTS-HTTP.md CONTRACTS-ONDISK.md` shows the updated lines, and confirm the check is RED before the fix (grep for the old "NOT gated" string must currently succeed).

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


- **blocks** [INVITE-GATE-ENFORCE](../INVITE-GATE-ENFORCE--8297d7e2/task.md)

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [INVITE-GATE-ENFORCE](../INVITE-GATE-ENFORCE--8297d7e2/task.md) — INVITE-GATE-ENFORCE: enforce invite-only enrolment (P0: anonymous roster exhaustion) (in_progress)
- [MTLS-CROSSCHECK](../../MTLS/MTLS-CROSSCHECK--2b2af075/task.md) — MTLS-CROSSCHECK: reject a session token presented over a connection whose client certific… (done)

## Referenced by other tasks (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [INVITE-GATE-ENFORCE](../INVITE-GATE-ENFORCE--8297d7e2/task.md) — INVITE-GATE-ENFORCE: enforce invite-only enrolment (P0: anonymous roster exhaustion) (in_progress)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
