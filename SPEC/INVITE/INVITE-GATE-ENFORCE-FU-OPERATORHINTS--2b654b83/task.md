# INVITE-GATE-ENFORCE-FU-OPERATORHINTS: fix README.md and bus-serve.sh operator hints that now 403

| Field | Value |
| --- | --- |
| Public id | `2b654b83-5b6d-4da1-a991-b04a4d183537` |
| Key | INVITE-GATE-ENFORCE-FU-OPERATORHINTS |
| Epic | [INVITE](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-14T22:57:00.865135+00:00 |
| Updated | 2026-08-14T22:57:00.865135+00:00 |
| Completed | — |

## Proof command

```sh
grep -n -- "--invite-file" README.md scripts/bus-serve.sh
```

## Description

README.md:113 and scripts/bus-serve.sh:410 both show or print an agent-busctl enrol ... --name NAME command with no invite. INVITE-GATE-ENFORCE (8297d7e2) makes every such call exit 4 (HTTP 403, invite-only enforcement). scripts/bus-serve.sh:410 is especially load-bearing: it prints this as the operators FIRST instruction at server startup, so as written it hands a brand-new operator a command guaranteed to fail. Fix both to reflect the real flow: agent-bus invite mint (bus stopped) to produce an invite file, then agent-busctl enrol --invite-file PATH --name NAME, plus the operational note that minting requires the bus stopped (it takes the data directory exclusive dirlock). Both files are clean in git -- verify before editing.

Proof: grep confirms README.md and bus-serve.sh no longer show a bare --name enrol with no invite, and instead reference --invite-file.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [INVITE-GATE-ENFORCE](../INVITE-GATE-ENFORCE--8297d7e2/task.md) — INVITE-GATE-ENFORCE: enforce invite-only enrolment (P0: anonymous roster exhaustion) (in_progress)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
