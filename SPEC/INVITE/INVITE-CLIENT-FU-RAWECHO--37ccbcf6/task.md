# INVITE-CLIENT-FU-RAWECHO: defence-in-depth -- several attacker-influenced strings reach the terminal without safeText, safe only by caller ordering today

| Field | Value |
| --- | --- |
| Public id | `37ccbcf6-68d1-431a-b2a4-d09bd04f6997` |
| Key | INVITE-CLIENT-FU-RAWECHO |
| Epic | [INVITE](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | cli |
| Section | backlog |
| Tags | — |
| Created | 2026-08-14T17:13:13.582375+00:00 |
| Updated | 2026-08-14T17:13:13.582375+00:00 |
| Completed | — |

## Proof command

```sh
go test -race -run TestUntrustedTextIsNeverEchoedRaw ./cmd/agent-busctl/ ./client/
```

## Description

DISCOVERED BY: reviewer/security gate during INVITE-CLIENT (4123e25d-4644-48e0-9ee1-e0a9a273fd8c)

Defence-in-depth for attacker-influenced text reaching the terminal. (a) `cmd/agent-busctl/root.go` (~:328, :343) echoes a positional argument VERBATIM into stderr and into `--json` -- newly likelier now that a pasted invite blob is a plausible mis-typed argument. (b) The `--invite-file` PATH is interpolated raw and unbounded into six `LoadInviteFile` messages. (c) The invite id is interpolated raw (not `safeText`'d) at `client/enrol.go:581`, `:595`, in `resolvePinsWith`'s invite branch, and at `client/invite.go:377/383/397/428/452` -- safe TODAY only because `Validate` always precedes on every reachable path, i.e. safety-by-caller-ordering rather than structurally. `annotateInviteRefusal` already applies the `safeText` the others should match.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [INVITE-CLIENT](../INVITE-CLIENT--4123e25d/task.md) — INVITE-CLIENT: the Go client/CLI redeems an invite at enrol (+ AGENT_PROTOCOL.md entry) -… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
