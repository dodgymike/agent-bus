# INVITE-GATE-ENFORCE-FU-BUSCTLTEST: fix TestCLIEnrolEndToEnd, now RED once the gate lands

| Field | Value |
| --- | --- |
| Public id | `837631a4-5e56-492c-a109-b0eae76e2e1c` |
| Key | INVITE-GATE-ENFORCE-FU-BUSCTLTEST |
| Epic | [INVITE](../epic.md) |
| Status | todo |
| Priority | P1 |
| Component | cli |
| Section | backlog |
| Tags | — |
| Created | 2026-08-14T22:57:16.963980+00:00 |
| Updated | 2026-08-14T22:57:16.963980+00:00 |
| Completed | — |

## Proof command

```sh
go test -race -run TestCLIEnrolEndToEnd ./cmd/agent-busctl/
```

## Description

cmd/agent-busctl/enrol_test.go TestCLIEnrolEndToEnd starts the real server binary and enrols anonymously (no invite). INVITE-GATE-ENFORCE (8297d7e2) enforces the invite gate, so an anonymous enrol now returns 403 and this test FAILS the moment INVITE-GATE-ENFORCE commits. A verified fix exists: start the bus once to create the certificate, stop it, run agent-bus invite mint -json to a 0600 file, restart the bus, then enrol via agent-busctl enrol --invite-file. This was prototyped and confirmed green OUTSIDE the repo; a patch is saved at /tmp/claude-1000/-mnt-sdb4-mike-mike-source-agent-bus/b828c013-a5a5-4da0-b21c-d56d21066f9e/scratchpad/busctl-enrol-test.patch (session-scoped scratchpad -- retrieve or re-derive it before this task is worked if the scratchpad has been cleaned). THIS TASK MUST BE SEQUENCED INTO THE SAME WAVE AS INVITE-GATE-ENFORCE COMMITTING -- cmd/agent-busctl/enrol_test.go is RED on main the moment that task lands, so this fix should land in the same commit or immediately after, not be left dangling as a broken test on main.

Proof: go test -race -run TestCLIEnrolEndToEnd ./cmd/agent-busctl/ passes, confirmed RED against the current anonymous-enrol version of the test before applying the fix.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [INVITE-GATE-ENFORCE](../INVITE-GATE-ENFORCE--8297d7e2/task.md) — INVITE-GATE-ENFORCE: enforce invite-only enrolment (P0: anonymous roster exhaustion) (in_progress)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
