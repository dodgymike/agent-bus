# INVITE-GATE-ENFORCE-FU-DECISIONS: add dated DECISIONS.md entry superseding the invite_required=false decision

| Field | Value |
| --- | --- |
| Public id | `a02d0684-2b8e-47a4-b6ce-76177101818c` |
| Key | INVITE-GATE-ENFORCE-FU-DECISIONS |
| Epic | [INVITE](../epic.md) |
| Status | todo |
| Priority | P1 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-14T22:55:28.405709+00:00 |
| Updated | 2026-08-14T22:55:28.405709+00:00 |
| Completed | — |

## Proof command

```sh
grep -c "enrolmentInviteRequired" DECISIONS.md | awk "{exit (\$1>0)?0:1}" && echo DECISIONS_UPDATED
```

## Description

DECISIONS.md ~4718-4812 holds a live dated decision (INVITE-GATE, 2026-08-14) stating invite_required stays FALSE "until (c) above is separately decided". INVITE-GATE-ENFORCE (8297d7e2) IS that decision -- it flips the gate on. Add a new dated section (do not edit the old one) covering three choices made in that task: (1) the flip itself -- auth.Service.Enrol now refuses req.Invite==nil with ErrInviteRequired, mapped to HTTP 403, placed ABOVE validation/idempotency/roster-cap/mint so no agent-id suffix is ever burned by an anonymous request (invariant 1); (2) enrolmentInviteRequired in cmd/agent-bus/main.go is a CONSTANT (true), deliberately with NO -require-invite flag -- a flag would be a documented, discoverable switch for turning invariant 3 off, which is exactly what the invariant forbids; (3) auth.Options.RequireInvite defaults FALSE in the internal/auth library itself -- reviewer noted the only real consumer is cmd/agent-bus (internal/ import-restricted to this module), so state that honestly rather than invoking a generic "embedder" justification that does not apply in this tree. DECISIONS.md is clean in git right now (verify before editing) -- append only, do not touch the existing INVITE-GATE section.

Proof: grep confirms a NEW dated section exists referencing the flip, the constant-not-flag choice, and the RequireInvite default, e.g. `grep -n "enrolmentInviteRequired" DECISIONS.md` finds the new section.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [INVITE-GATE](../INVITE-GATE--05a5216d/task.md) — INVITE-GATE: POST /v1/enroll REQUIRES a valid invite and fails closed; invite consumption… (done)
- [INVITE-GATE-ENFORCE](../INVITE-GATE-ENFORCE--8297d7e2/task.md) — INVITE-GATE-ENFORCE: enforce invite-only enrolment (P0: anonymous roster exhaustion) (in_progress)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
