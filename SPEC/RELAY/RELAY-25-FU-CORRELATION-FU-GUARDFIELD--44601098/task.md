# RELAY-25-FU-CORRELATION-FU-GUARDFIELD: the 'no message_id on non-record lines' guard names the wrong field now

| Field | Value |
| --- | --- |
| Public id | `44601098-7bfd-4abf-b2fe-30d08ba7431d` |
| Key | _(null in the export)_ |
| Epic | [RELAY](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | tooling |
| Section | backlog |
| Tags | — |
| Created | 2026-08-15T14:07:55.000387+00:00 |
| Updated | 2026-08-15T14:07:55.000387+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/proof-check.sh 'go test -race -run TestAuditLogNonRecordObjectsCarryNoCorrelationKeys ./cmd/agent-bus'
```

## Description

`cmd/agent-bus/auditlog.go:358-360` and `CONTRACTS-CLI.md:924` both state the contract that no non-record `--json` object may carry a `message_id` key, justified by "scripts/fed-smoke.sh counts objects matching a message id". Since RELAY-25-FU-CORRELATION the script's primary correlator is `content_sha256`, not `message_id`, and CONTRACTS-CLI.md additionally cites `assert_audit_path`, a function that task DELETED (it is now `assert_audit_hop` / `audit_origin_digest`).

NOTHING IS BROKEN TODAY -- verified: `auditLogDamage` and `auditLogError` carry neither key. The defect is that the guard comment names the wrong field, so a future damage/error shape gaining a `content_sha256` key would silently corrupt fed-smoke's exactly-one correlated-record count with no test catching it. Raised independently by BOTH the reviewer and security gates.

FIX: state the rule as "no object other than a record ever carries a `message_id` OR a `content_sha256` key", update the cited function names, and add a test that pins it (marshal auditLogDamage/auditLogError and assert neither key is present) so the guard stops being comment-only.

NOTE: CONTRACTS-CLI.md was ALSO contaminated by a concurrent deployment agent's uncommitted DEPLOY-6 section during RELAY-25-FU-CORRELATION; check `git diff HEAD -- CONTRACTS-CLI.md` before editing.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


- **relates to** [RELAY-25-FU-CORRELATION](../RELAY-25-FU-CORRELATION--3f009222/task.md)

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [DEPLOY-6](../../DEPLOY/DEPLOY-6--e12b75cd/task.md) — DEPLOY-6: host-reachable Dockerfile CMD + THREE-BUS-DOCKER.md federated runbook (done)
- [RELAY-25-FU-CORRELATION](../RELAY-25-FU-CORRELATION--3f009222/task.md) — RELAY-25-FU-CORRELATION: fed-smoke.sh asserts the SAME message_id string in A's, B's and… (in_progress)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
