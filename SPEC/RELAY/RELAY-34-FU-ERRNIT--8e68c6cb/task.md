# RELAY-34-FU-ERRNIT: a comment names ErrPeerBusIDCollision where the refusal may now be ErrPeerWithdrawn

| Field | Value |
| --- | --- |
| Public id | `8e68c6cb-02ff-491a-a5b9-b11fa99c028d` |
| Key | RELAY-34-FU-ERRNIT |
| Epic | [RELAY](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | relay |
| Section | backlog |
| Tags | follow-up, relay-34 |
| Created | 2026-08-08T21:48:20.712901+00:00 |
| Updated | 2026-08-08T21:48:20.712901+00:00 |
| Completed | — |

## Description

Reviewer-gate round-4 non-blocking nit on RELAY-34, recorded so it is not lost.

In internal/relay/peerstore.go, the reconcileWithdrawalFloor confusable comment argues the residual is 'NOT a new un-pinning' on the grounds that the legitimate bus is refused by ErrPeerBusIDCollision either way. The CLAIM is correct and was verified by enumeration, but the MECHANISM named is imprecise: after RELAY-34 the sentinel actually met may be ErrPeerWithdrawn (the floor check in busTable.upsert runs BEFORE the case-collision check) or the sweptMax admission floor, depending on state.

Comment-only correction. Worth doing because this file's comments are load-bearing -- reviewers have repeatedly used them as the specification -- and a comment naming the wrong sentinel is exactly the kind of thing a later reader trusts.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-34](../RELAY-34--03fd8897/task.md) — RELAY-34: Revocation fails OPEN on a WAL discard -- a revoked pinned bus signing key can… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
