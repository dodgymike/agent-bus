# fed-smoke.sh: add_route/add_trust discard stdout and never require_ok, so a dropped fingerprint arg silently reverts to the exact invisible failure RELAY-25-FU-INBOUNDBIND just fixed

| Field | Value |
| --- | --- |
| Public id | `db3047c0-13dd-4885-8f8f-5d86adf023ee` |
| Key | _(null in the export)_ |
| Epic | [RELAY](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | relay |
| Section | backlog |
| Tags | reviewer-finding, non-blocking |
| Created | 2026-08-15T07:51:35.239188+00:00 |
| Updated | 2026-08-15T07:51:35.239188+00:00 |
| Completed | — |

## Proof command

```sh
bash -n scripts/fed-smoke.sh && grep -n 'peer list -json' scripts/fed-smoke.sh | grep -q .
```

## Description

Raised by the reviewer as a non-blocking advisory (c) on RELAY-25-FU-INBOUNDBIND (336c3b76-6fab-478e-af3d-af99dde597fd, now done): add_route and add_trust in scripts/fed-smoke.sh send their command's stdout to /dev/null and never call require_ok, and nothing in the script asserts the bindings actually landed. A future edit that drops an fp_* argument (e.g. `add_trust "$SERVER_B" "$DATA_B" "$bus_a" "$signing_a"` instead of `... "$signing_a" "$fp_a"`) silently reverts to the exact invisible failure 336c3b76 just fixed -- bindablePeerCount() drops back to 0 -- and it would surface roughly 100 lines later as an unrelated-looking exit 7 at the /v1/send step, not at the point of the actual mistake. That is precisely how the original defect hid for as long as it did.

Wants: after peer configuration (add_route/add_trust) completes for all three buses, assert via `peer list -json` that each bus shows the expected inbound client-certificate bindings (and outbound route fingerprints) -- i.e. fail loudly and immediately at the configuration step, not silently 100 lines downstream.

Scope: scripts/fed-smoke.sh only, mirroring the exact pattern used elsewhere in the script for require_ok-style assertions.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-25-FU-INBOUNDBIND](../RELAY-25-FU-INBOUNDBIND--336c3b76/task.md) — RELAY-25-FU-INBOUNDBIND: fed-smoke.sh never binds each peer's INBOUND client-certificate… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
