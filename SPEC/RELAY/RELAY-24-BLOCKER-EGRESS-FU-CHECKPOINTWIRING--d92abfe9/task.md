# RELAY-24-BLOCKER-EGRESS-FU-CHECKPOINTWIRING: Outbox.Checkpoint has no production caller -- checkpoint dispatcher entirely unwired

| Field | Value |
| --- | --- |
| Public id | `d92abfe9-6f3d-4ca4-91fa-90b044eff0b3` |
| Key | _(null in the export)_ |
| Epic | [RELAY](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | relay |
| Section | backlog |
| Tags | — |
| Created | 2026-08-15T10:14:16.804179+00:00 |
| Updated | 2026-08-15T10:14:16.804179+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/proof-check.sh 'go test -race -run TestOutboxCheckpointReclaimsRetainedRecordsInProduction ./internal/relay'
```

## Description

P3. Retention is now CORRECT without a checkpoint caller -- RELAY-24-BLOCKER-EGRESS (85ae8b32-3a46-4e85-bdfe-ea29730670fb) fixed the reclaim wedge by adding wal.Log.CheckpointSupported() and having the outbox ask capability rather than type. This task is the standing gap that caused the wedge, filed for its own sake because it is still entirely unwired: wal.Open is called with no LogOptions.Checkpoints, so Log.Checkpoint() unconditionally returns "checkpoint requires a MultiApplier", and Outbox.Checkpoint() is dead code -- nothing in cmd/agent-bus's composition root ever calls it.

Record the P0 this caused, as evidence for why the gap matters even though it is now papered over: because *wal.Log satisfies interface{ Checkpoint() error } while being permanently unable to run one, the outbox deferred reclaiming retained records "until the next checkpoint" -- i.e. forever -- and cross-bus egress to a peer wedged permanently at the per-peer retained limit. Measured: 256 retained records, then ErrOutboxCapacity for the life of the process, with every tombstone provably 48h past a 24h retention window.

DEFINITION OF DONE: wal.Open in cmd/agent-bus's composition root is given a real LogOptions.Checkpoints (a MultiApplier), Log.Checkpoint() actually runs and reclaims, and Outbox.Checkpoint() gets a real production caller (on a schedule, or triggered by the same capacity pressure that currently falls back to CheckpointSupported()==false). Do not just make the type-assertion succeed -- prove reclaim actually happens under retention pressure.

Invariants to read: 4 (never trade durability for latency -- a checkpoint must not violate this), 5 (memory serving copy / disk truth), 6 (log records metadata/routing only, recovery always reaches a running server).

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


- **relates to** [RELAY-24-BLOCKER-EGRESS](../RELAY-24-BLOCKER-EGRESS--85ae8b32/task.md)

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-24-BLOCKER-EGRESS](../RELAY-24-BLOCKER-EGRESS--85ae8b32/task.md) — RELAY-24-BLOCKER-EGRESS: a bus SENDING a relayed message has no wiring at all -- relay.Ne… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
