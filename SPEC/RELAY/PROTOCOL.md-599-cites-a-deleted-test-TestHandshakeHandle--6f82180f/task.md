# PROTOCOL.md:599 cites a deleted test (TestHandshakeHandlerIsNotWiredIntoAnyMux) as a live guard

| Field | Value |
| --- | --- |
| Public id | `6f82180f-8f57-473d-bd87-30f6d9d9695d` |
| Key | _(null in the export)_ |
| Epic | [RELAY](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-15T11:25:33.852826+00:00 |
| Updated | 2026-08-15T11:25:33.852826+00:00 |
| Completed | — |

## Proof command

```sh
test $(grep -c 'TestHandshakeHandlerIsNotWiredIntoAnyMux' PROTOCOL.md) -eq 0 && grep -n 'TestRelayImportedOnlyByWiringSites' PROTOCOL.md | grep -q .
```

## Description

PROTOCOL.md:599 still describes `internal/relay`'s isolation from any mux as enforced by "a guard test, `TestHandshakeHandlerIsNotWiredIntoAnyMux` in `internal/relay/guards_test.go`, [that] walks the repository and fails if any other package imports it". That test NO LONGER EXISTS -- RELAY-18 retired it deliberately (the blanket import ban could not survive the composition root once cmd/agent-bus and internal/httpapi needed to import internal/relay for real wiring). All that remains at its old location is a retirement note: internal/relay/guards_test.go:231, doc comment on TestRelayImportedOnlyByWiringSites, which explains explicitly 'TestRelayImportedOnlyByWiringSites replaces the blanket import ban that TestHandshakeHandlerIsNotWiredIntoAnyMux used to enforce. RELAY-18 retired that ban DELIBERATELY... The old rule was replaced, not deleted.'

Confirmed RED at filing time (2026-08-15): `grep -n TestHandshakeHandlerIsNotWiredIntoAnyMux PROTOCOL.md` returns exactly one hit, at line 599, and `grep -rn '^func TestHandshakeHandlerIsNotWiredIntoAnyMux' internal/relay/` returns nothing -- the function does not exist anywhere in the tree. So the citation is currently false: it presents a deleted guard as live.

This is the SAME defect class the RELAY-24-BLOCKER-EGRESS reviewer already found and fixed in two other files this pass: CONTRACTS-ONDISK.md (see the 'And the forwarder is now wired, not merely built' passage, commit e1bd842) says explicitly 'An earlier draft of this paragraph said... and cited `TestHandshakeHandlerIsNotWiredIntoAnyMux` as evidence... -- that test no longer exists', and a second file was corrected the same pass (per the RELAY-24-BLOCKER-EGRESS-HANDSHAKE task journal). PROTOCOL.md is the one remaining stale citation of the three the reviewer found.

FIX: replace the PROTOCOL.md:599 citation with the CURRENT guard -- TestRelayImportedOnlyByWiringSites (internal/relay/guards_test.go), which enforces the narrower rule (importable only by internal/httpapi and cmd/agent-bus, per RELAY-18) -- and, following CONTRACTS-ONDISK.md's pattern, say explicitly that the old blanket-ban test no longer exists rather than silently swapping the name (so a future reader does not wonder whether the guarantee itself narrowed, only that the enforcing test's identity did). Pre-existing drift, not introduced by and not in scope of RELAY-24-BLOCKER-EGRESS -- filed separately per the reviewer's finding while verifying that task's own doc fixes.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


- **relates to** [RELAY-24-BLOCKER-EGRESS](../RELAY-24-BLOCKER-EGRESS--85ae8b32/task.md)

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-18](../RELAY-18--fa5d1b0d/task.md) — RELAY-18: Retire the relay import guard deliberately, replaced by a narrower one (done)
- [RELAY-24-BLOCKER-EGRESS](../RELAY-24-BLOCKER-EGRESS--85ae8b32/task.md) — RELAY-24-BLOCKER-EGRESS: a bus SENDING a relayed message has no wiring at all -- relay.Ne… (done)
- [RELAY-24-BLOCKER-EGRESS-HANDSHAKE](../RELAY-24-BLOCKER-EGRESS-HANDSHAKE--0ab31d26/task.md) — RELAY-24-BLOCKER-EGRESS-HANDSHAKE: this bus never DIALS a peer, so its relay Registry nev… (todo)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
