# RELAY-25-FU-CORRELATION-FU-WATCHLINK: link the recipient's watch record to C's audit record by C's own minted id

| Field | Value |
| --- | --- |
| Public id | `037a9860-1e3a-4447-a0c5-0f05c696eff1` |
| Key | _(null in the export)_ |
| Epic | [RELAY](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | tooling |
| Section | backlog |
| Tags | — |
| Created | 2026-08-15T14:07:55.280172+00:00 |
| Updated | 2026-08-15T14:07:55.280172+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/proof-check.sh 'bash scripts/fed-smoke.sh --self-test'
```

## Description

LOW-1 from the security gate on RELAY-25-FU-CORRELATION. fed-smoke.sh correlates the `agent-busctl watch` delivery stream on text+from+to+bus_path and correlates the three audits on `content_sha256`, but the two surfaces are never linked to each other. Both are C-LOCAL, so an exact link is cheaply available and sound: `watchRecord.MessageID` comes from `client.Message.MessageID` and `wal.AuditRecord.MessageID` comes from `m.ID` -- the same store.Message on C -- so capture `.message_id` from the matched watch record and assert it equals the `message_id` of C's audit record carrying the correlated digest. This proves the message the RECIPIENT RECEIVED is the same record C AUDITED, which is currently only inferred.

NOT a regression: the old link used bus A's id, which C can never hold (invariant 1).

Must be shown RED before green (doctor the fixture so the ids differ).

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


- **relates to** [RELAY-25-FU-CORRELATION](../RELAY-25-FU-CORRELATION--3f009222/task.md)

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-25-FU-CORRELATION](../RELAY-25-FU-CORRELATION--3f009222/task.md) — RELAY-25-FU-CORRELATION: fed-smoke.sh asserts the SAME message_id string in A's, B's and… (in_progress)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
