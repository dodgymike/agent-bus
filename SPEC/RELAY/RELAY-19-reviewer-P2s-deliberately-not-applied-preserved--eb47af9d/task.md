# RELAY-19 reviewer P2s deliberately not applied (preserved an md5-pinned PASS) -- apply the three

| Field | Value |
| --- | --- |
| Public id | `eb47af9d-5342-4944-87e8-94f5e2399e8f` |
| Key | _(null in the export)_ |
| Epic | [RELAY](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | relay |
| Section | backlog |
| Tags | deferred-by-design |
| Created | 2026-08-14T10:07:09.386179+00:00 |
| Updated | 2026-08-14T10:07:09.386179+00:00 |
| Completed | — |

## Proof command

```sh
go build ./... && go vet ./internal/relay && go test -race ./internal/relay
```

## Description

Three P2 findings from RELAY-19's reviewer re-review were DELIBERATELY left unapplied so the gate's md5-pinned PASS verdict (against forward.go md5 4ae81916010b4c69da32112d4c74817c / crashloop_test.go md5 813a9898784a5446bed992ebd913d144) would remain valid evidence rather than being invalidated by a post-review edit. Recorded here explicitly per the requester's instruction: these were SKIPPED DELIBERATELY, not overlooked, and this task exists so they are not lost.

1. resumeJob's loop arm raises Dropped.Loop without placing it in the DropCounts counter header doc comment (the header at forward.go:120-134 enumerates the other drop causes but omits this one from the printed/documented list even though the field exists and is incremented).

2. ErrOutboxInFlight's Warn wording is imprecise when the in-flight writer turns out to be a Settle rather than an Enqueue -- the log line's phrasing assumes the racing writer is another Enqueue and should be reworded to cover both cases or made outcome-agnostic.

3. The forward-before-Resume() ordering signal is currently log-only (a Warn/Info line); a ForwarderStats field (e.g. Resumed bool / ResumedAt time.Time) would make the precondition queryable/testable rather than only observable via log-scraping, which would help RELAY-24's wiring task prove its own ordering constraint (see RELAY-24's description for the three-stage startup sequence this stat would help verify).

All three are cosmetic/observability, not correctness: RELAY-19's security and reviewer gates both PASSED with these open. Apply in one small pass against internal/relay/forward.go once RELAY-19 is safely on main and this task's changes can be reviewed against a fresh state rather than mid-flight.

Source: RELAY-19 file-followups brief item 4 (second bullet), 2026-08-14.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-19](../RELAY-19--24e0bd11/task.md) — RELAY-19: Forwarder writes and settles outbox records (part 2 of 2) (done)
- [RELAY-24](../RELAY-24--e303c624/task.md) — RELAY-24: Composition root: wire federation into cmd/agent-bus/main.go (todo)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
