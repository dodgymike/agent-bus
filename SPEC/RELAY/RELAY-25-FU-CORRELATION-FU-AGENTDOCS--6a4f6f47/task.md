# RELAY-25-FU-CORRELATION-FU-AGENTDOCS: CONTRACTS-AGENT.md still says fed-smoke.sh is 'expected to fail'

| Field | Value |
| --- | --- |
| Public id | `6a4f6f47-bd00-452b-ac0f-049c4b7b3ec0` |
| Key | _(null in the export)_ |
| Epic | [RELAY](../epic.md) |
| Status | todo |
| Priority | P1 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-15T14:07:54.663669+00:00 |
| Updated | 2026-08-15T14:10:26.846189+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/proof-check.sh '! grep -n "fed-smoke.sh" CONTRACTS-AGENT.md | grep -qiE "expected to fail|fails loudly at the first unavailable step"'
```

## Description

CONTRACTS-AGENT.md lines 29 and 104 (as of HEAD b700d8e) describe `scripts/fed-smoke.sh` as "Deliberately describes the surface as it SHOULD be, so it fails loudly at the first unavailable step" and "currently expected to fail at the first unavailable step". BOTH ARE NOW FALSE: as of RELAY-25-FU-CORRELATION the script exits 0 and is the RELAY epic's green deliverable (proof-check verdict=PASS class=wrapper exit=0 in a clean overlay of HEAD). Flagged as a BLOCKER by the reviewer gate on RELAY-25-FU-CORRELATION.

WHY IT WAS NOT FIXED IN THAT TASK: CONTRACTS-AGENT.md was carrying 44 uncommitted lines from a CONCURRENT agent's CONTEXT-DOCCHECK work (a `scripts/doc-check.sh` section). A pathspec commit takes the WORKTREE, so editing lines 29/104 would have shipped that other agent's ungated section under RELAY-25-FU-CORRELATION's commit title -- the exact failure mode CLAUDE.md documents. The two edits are textually disjoint, so this is purely a sequencing constraint: apply it once the doc-check work has landed.

REQUIRED: before editing, run `git status --porcelain -- CONTRACTS-AGENT.md` and `git diff HEAD -- CONTRACTS-AGENT.md` and confirm every hunk is yours.

PROOF NOTE (test-engineer, 2026-08-15): proof_cmd was replaced. The original pinned only the line-104 wording and was measured GREEN on a half-fix that left line 29 stale, plus its first clause was an incidental-match grep. The replacement pins BOTH stale phrases and was verified RED on the stale file, GREEN after both lines are fixed, and RED on a line-104-only fix. Both lines must be corrected: line 29 currently says the script 'fails loudly at the first unavailable step' and line 104 says it is 'currently expected to fail' -- as of RELAY-25-FU-CORRELATION the script exits 0.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


- **blocks** [RELAY-25-FU-CORRELATION](../RELAY-25-FU-CORRELATION--3f009222/task.md)
- **relates to** [RELAY-25-FU-CORRELATION](../RELAY-25-FU-CORRELATION--3f009222/task.md)

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [CONTEXT-DOCCHECK](../../CONTEXT/CONTEXT-DOCCHECK--b3b28f45/task.md) — CONTEXT-DOCCHECK: doc-check.sh -- the instrument every other proof in this epic depends on (done)
- [RELAY-25-FU-CORRELATION](../RELAY-25-FU-CORRELATION--3f009222/task.md) — RELAY-25-FU-CORRELATION: fed-smoke.sh asserts the SAME message_id string in A's, B's and… (done)

## Referenced by other tasks (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-25](../RELAY-25--10491a01/task.md) — RELAY-25: fed-smoke.sh: the epic's deliverable -- three-bus loopback federation smoke test (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
