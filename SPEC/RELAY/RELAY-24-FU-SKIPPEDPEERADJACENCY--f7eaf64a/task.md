# RELAY-24-FU-SKIPPEDPEERADJACENCY: skippedPeerRecords ERROR block is wedged inside the comment that documents walLog.Recovered()

| Field | Value |
| --- | --- |
| Public id | `f7eaf64a-31aa-4b34-a1f2-57c559263cad` |
| Key | _(null in the export)_ |
| Epic | [RELAY](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | relay |
| Section | backlog |
| Tags | reviewer-finding, non-blocking |
| Created | 2026-08-15T07:47:13.316342+00:00 |
| Updated | 2026-08-15T07:47:13.316342+00:00 |
| Completed | — |

## Proof command

```sh
go test -race -run TestSkippedPeerRecordsBlockNotWedgedInRecoveryComment ./cmd/agent-bus/...
```

## Description

cmd/agent-bus/main.go: the skippedPeerRecords ERROR block at :646 sits between an 18-line comment starting at :629 and the walLog.Recovered() statement that comment actually documents. Purely structural (no behaviour change) -- move the ERROR block so the comment is immediately followed by the code it describes. Worth filing only because a future reader will trust the adjacency and misattribute the comment to the wrong statement (invariant 6's loud-and-specific logging bar extends to the surrounding code being honestly organised, not just the log text itself).

Found during the reviewer's pass on RELAY-24 (e303c624-3062-446d-9efc-86a9284220d3, "Composition root: wire federation into cmd/agent-bus/main.go"); filed as a non-blocking P4 (lowest) finding from that review.

PROOF: names a Go test to ADD (it does not exist yet) -- TestSkippedPeerRecordsBlockNotWedgedInRecoveryComment in cmd/agent-bus. It should parse cmd/agent-bus/main.go with go/parser+go/ast, locate the comment group documenting the walLog.Recovered() call, and assert no other statement (specifically not the skippedPeerRecords handling) lies between the comment group's end position and the documented statement's start position. MUST be observed RED before the fix (today the skippedPeerRecords block IS between them, so the adjacency assertion fails / the test does not yet exist) and GREEN once the block is moved.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-24](../RELAY-24--e303c624/task.md) — RELAY-24: Composition root: wire federation into cmd/agent-bus/main.go (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
