# RELAY-24-FU-STOREMSGLOOKUP-DOCS: CONTRACTS-ONDISK.md + DECISIONS.md + AGENT_LOG.md entries for the store lookup / OriginMessageID change

| Field | Value |
| --- | --- |
| Public id | `e02aa062-a0ec-48b6-9f39-eeee64801580` |
| Key | _(null in the export)_ |
| Epic | [RELAY](../epic.md) |
| Status | done |
| Priority | P1 |
| Component | docs |
| Section | backlog |
| Tags | — |
| Created | 2026-08-15T07:09:42.777218+00:00 |
| Updated | 2026-08-15T07:25:15.758144+00:00 |
| Completed | 2026-08-15T07:25:15.758126+00:00 |

## Proof command

```sh
bash scripts/proof-check.sh 'grep -qF "json:\"origin_message_id,omitempty\"" CONTRACTS-ONDISK.md && grep -qF "RecordVersion\` DELIBERATELY STAYS AT 2" CONTRACTS-ONDISK.md && echo DOCS_OK'
```

## Description

Documentation debt for RELAY-24-FU-STOREMSGLOOKUP (c6530638-7cca-4404-bc61-88ca6c2d30b9), BLOCKED BY THAT TASK'S FILE BOUNDARY, not skipped by choice. The reviewer made the CONTRACTS-ONDISK.md update a COMPLETION CONDITION for that task, so this task BLOCKS it going `done` -- do not let it close without this landing (or an explicit, recorded decision to waive it).

THREE required edits:

1. CONTRACTS-ONDISK.md: document the new optional durable record field `origin_message_id`, PLUS an explicit statement that RecordVersion stays at 2 and WHY. The current mentions of an `origin_message_id`-shaped key belong only to the relay OUTBOX record, not the store MESSAGE record -- this is a genuinely new field on a different record type and needs its own documentation.

2. DECISIONS.md: a dated entry for the no-version-bump decision. Preserve this reasoning VERBATIM in the task description (and carry it into the DECISIONS.md entry) because it is counter-intuitive and someone will otherwise "fix" it later:

   "Record.Decode does an EXACT version match, so bumping RecordVersion to 3 would discard all existing message history on upgrade. An added OPTIONAL field does not move the version -- RecordVersion's own doc says so, and Record decoding is non-strict about unknown fields. Bumping would have been actively harmful."

   Also add a second dated DECISIONS.md entry for the peer-triggerable duplicate-origin resolution: Store.Append's duplicate-OriginMessageID branch retains BOTH messages (last-writer-wins on the index), returns nil (no error), logs the event ONCE per process (log-once + counter, mirroring hub.idemCapWarned) and increments DuplicateOriginMessageIDs() on every occurrence. This branch is PEER-TRIGGERABLE (not merely a local-bug indicator as an earlier, now-corrected code comment claimed) -- see security's M1 finding on that task: the relay applied-key idempotency scope is the triple (sender, OpRelay, origin message id), the sender label is PEER-ASSERTED, and hub.relayedOrigin binds only the bus halves of sender/origin, never the agent half -- so one peer can present the same origin id under two attested sender labels within its OWN namespace and reach this branch legitimately, with the durable idempotency memory working exactly as designed. Returning an error instead would orphan an already-committed-and-fsynced record (invariant 4 / the SIGN-1-FU-OUTOFORDER-POISON precedent) -- retain+log+count is correct, not a compromise.

3. AGENT_LOG.md: the work-log entry for RELAY-24-FU-STOREMSGLOOKUP itself (implementer/test-engineer/reviewer/security chain, 2026-08-14/15, code-complete + both gates PASS, not yet committed at time of filing).

Relations: this task BLOCKS RELAY-24-FU-STOREMSGLOOKUP (c6530638-7cca-4404-bc61-88ca6c2d30b9) going `done`.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


- **blocks** [RELAY-24-FU-STOREMSGLOOKUP](../RELAY-24-FU-STOREMSGLOOKUP--c6530638/task.md)

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-24-FU-STOREMSGLOOKUP](../RELAY-24-FU-STOREMSGLOOKUP--c6530638/task.md) — RELAY-24-FU-STOREMSGLOOKUP: internal/store needs a lookup-by-message-id and an OriginMess… (done)
- [SIGN-1-FU-OUTOFORDER-POISON](../../SIGN/SIGN-1-FU-OUTOFORDER-POISON--bbd81523/task.md) — SIGN-1-FU-OUTOFORDER-POISON: Reserve-then-send lets mints be spent out of order, which pe… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
