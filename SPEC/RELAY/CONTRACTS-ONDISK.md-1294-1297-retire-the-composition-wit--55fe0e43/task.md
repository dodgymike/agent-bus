# CONTRACTS-ONDISK.md:1294-1297: retire the "composition with the forwarder remains RELAY-19" clause without touching its still-true neighbour

| Field | Value |
| --- | --- |
| Public id | `55fe0e43-3cd1-40a6-8f18-73f0fbea69d3` |
| Key | _(null in the export)_ |
| Epic | [RELAY](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | docs |
| Section | backlog |
| Tags | docs-stale |
| Created | 2026-08-14T10:06:58.742650+00:00 |
| Updated | 2026-08-14T10:06:58.742650+00:00 |
| Completed | — |

## Proof command

```sh
grep -n "composition with the forwarder remains RELAY-24" CONTRACTS-ONDISK.md && grep -n "cross-bus delivery remains BEST EFFORT from a running server" CONTRACTS-ONDISK.md
```

## Description

CONTRACTS-ONDISK.md:1294-1297 currently reads:

  described below; composition with the forwarder remains RELAY-19. Until that wiring lands,
  cross-bus delivery remains BEST EFFORT from a running server even though the durable mechanism now
  exists as a library.

As of RELAY-19 (feature-runner report, 2026-08-14), the FIRST clause -- "composition with the
forwarder remains RELAY-19" -- is now STALE: RELAY-19 itself is done (or in_progress/committed by
the time this is picked up), and the Forwarder now writes and settles durable outbox records. The
SECOND sentence -- "cross-bus delivery remains BEST EFFORT from a running server" -- is STILL TRUE
and MUST NOT be changed by whoever fixes the first: NewForwarder still has zero production callers
(nothing wires Outbox+RecoverMessage into it or calls Resume() from cmd/agent-bus yet -- that is
RELAY-24, still open). Composition into a running server, not the outbox mechanism itself, is what
remains outstanding.

Fix: update the first clause to name RELAY-24 (composition root: wire federation into
cmd/agent-bus/main.go) as the still-open wiring task, and leave the BEST-EFFORT sentence exactly as
is -- it is correct today and will only stop being correct once RELAY-24 lands and is verified
live, not merely committed. Do not mark cross-bus delivery as anything other than best-effort until
RELAY-24 is itself closed with a live-verification proof, per this repo's code-complete-vs-deployed
distinction.

Source: RELAY-19 file-followups brief item 4 (first bullet), 2026-08-14.

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


_None recorded._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-19](../RELAY-19--24e0bd11/task.md) — RELAY-19: Forwarder writes and settles outbox records (part 2 of 2) (done)
- [RELAY-24](../RELAY-24--e303c624/task.md) — RELAY-24: Composition root: wire federation into cmd/agent-bus/main.go (todo)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
