# RELAY-15-FU-CAPACITY-FAIRNESS: Outbox capacity is a 24h throughput ceiling and is not per-peer fair

| Field | Value |
| --- | --- |
| Public id | `4fd2d8d7-68c7-4488-b268-ed7ab2e849cc` |
| Key | _(null in the export)_ |
| Epic | [RELAY](../epic.md) |
| Status | done |
| Priority | P1 |
| Component | relay |
| Section | backlog |
| Tags | — |
| Created | 2026-08-08T21:35:05.339655+00:00 |
| Updated | 2026-08-09T00:07:28.225544+00:00 |
| Completed | 2026-08-09T00:07:28.225528+00:00 |

## Proof command

```sh
go test -race -run TestOutboxCapacity ./internal/relay
```

## Status note

P0 gate failure: replay applies the new per-peer quota and can discard previously acknowledged pending records from a pre-upgrade log. Required minimal fix: enforce the per-peer quota only for new live admission; replay preserves all valid durable pending records within the global bound, then initializes counters from that state. Add deterministic upgrade/replay proof. P1 tombstone resource bound is split to 617ffe5a-db42-4aeb-89bb-d9b0889f6c19. This task remains in_progress; RELAY-19 stays blocked.

## Description

MaxOutboxJobs (= MaxPeers 64 x DefaultQueueDepth 256 = 16,384) formerly counted retained pending records plus 24h tombstones, giving a global ~0.19/s ceiling and allowing one peer to halt relay. This task changes LIVE admission to bound pending durable work globally and fairly per peer while retaining tombstones for the full anti-resurrection window.

P0 ACCEPTANCE ADDED 2026-08-08: the per-peer quota is a live-admission rule only. Replay MUST reconstruct every acknowledged durable pending record that is within the existing global bound, even if an old/pre-upgrade log has more than the current per-peer share. Replay may never discard such work merely to apply a new fairness policy; doing so loses acknowledged work through this bus recovery path (invariants 4/5). Add a deterministic upgrade/replay regression test and ensure pending counters are initialized to the reconstructed durable state before new admission begins.

Per-peer fsync reservations must prevent one peer overshooting its live share while another peer remains admissible. RELAY-19 remains blocked until this task is gated and committed. Tombstone count/byte resource bounding is deliberately split to a separately blocking task: it must preserve the full anti-resurrection meaning, not evict/reject terminal records as a shortcut.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [617ffe5a-db42-4aeb-89bb-d9b0889f6c19](../Bound-retained-outbox-tombstone-resources-without-reopen--617ffe5a/task.md) — Bound retained outbox tombstone resources without reopening replay resurrection (done)
- [RELAY-19](../RELAY-19--24e0bd11/task.md) — RELAY-19: Forwarder writes and settles outbox records (part 2 of 2) (done)

## Referenced by other tasks (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [617ffe5a-db42-4aeb-89bb-d9b0889f6c19](../Bound-retained-outbox-tombstone-resources-without-reopen--617ffe5a/task.md) — Bound retained outbox tombstone resources without reopening replay resurrection (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
