# RELAY-24-BLOCKER-EGRESS-FU-FEDSMOKE: three-bus federation smoke test (fed-smoke.sh, both directions) was not run for the egress task

| Field | Value |
| --- | --- |
| Public id | `3e96dae2-19d7-4a2e-94c8-ad0e26fd5519` |
| Key | _(null in the export)_ |
| Epic | [RELAY](../epic.md) |
| Status | done |
| Priority | P1 |
| Component | relay |
| Section | backlog |
| Tags | — |
| Created | 2026-08-15T10:45:18.295532+00:00 |
| Updated | 2026-08-16T10:35:31.000453+00:00 |
| Completed | 2026-08-16T10:35:31.000435+00:00 |

## Proof command

```sh
bash scripts/fed-smoke.sh
```

## Description

RELAY-24-BLOCKER-EGRESS's description requires "the three-bus federation smoke test (fed-smoke.sh) exercising BOTH directions". Two of its three test clauses were delivered (the end-to-end forwarder-reached test, TestLocalMessageForPeerRecipientReachesForwarder, and the crash-injection ordering test, TestForwarderResumeOrderingSurvivesCrash); this third clause was NOT run and must not be silently dropped. As of filing: scripts/fed-smoke.sh was held by another agent throughout RELAY-24-BLOCKER-EGRESS's work, the egress code (cmd/agent-bus/relayegress.go, relaydial.go, internal/relay/forward.go, registry.go) is not yet committed, and the reviewer explicitly ruled a truthful test_summary for the parent task MAY NOT assert anything about fed-smoke.sh or three-bus federation. This task exists to run it once the egress code lands and to record the result honestly -- do not complete RELAY-24-BLOCKER-EGRESS's own test_summary as if this ran. Also depends on RELAY-24-BLOCKER-PEERCERTFLAG (0e6b5a49) per RELAY-24-BLOCKER-EGRESS's own TESTS clause, since fed-smoke.sh needs a bindable peer on both sides to prove anything.

From the reviewer re-verification of RELAY-24-BLOCKER-EGRESS (85ae8b32-3a46-4e85-bdfe-ea29730670fb).

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-24-BLOCKER-EGRESS](../RELAY-24-BLOCKER-EGRESS--85ae8b32/task.md) — RELAY-24-BLOCKER-EGRESS: a bus SENDING a relayed message has no wiring at all -- relay.Ne… (done)
- [RELAY-24-BLOCKER-PEERCERTFLAG](../RELAY-24-BLOCKER-PEERCERTFLAG--0e6b5a49/task.md) — RELAY-24-BLOCKER-PEERCERTFLAG: agent-bus peer add has no flag to bind a peer's inbound cl… (done)

## Referenced by other tasks (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-25-FU-CORRELATION](../RELAY-25-FU-CORRELATION--3f009222/task.md) — RELAY-25-FU-CORRELATION: fed-smoke.sh asserts the SAME message_id string in A's, B's and… (in_progress)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
