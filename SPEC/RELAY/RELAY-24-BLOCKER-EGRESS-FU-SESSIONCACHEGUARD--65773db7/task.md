# RELAY-24-BLOCKER-EGRESS-FU-SESSIONCACHEGUARD: AST guard gap over the new outbound dial path (cmd/agent-bus not covered)

| Field | Value |
| --- | --- |
| Public id | `65773db7-82e3-4f9e-9f66-b4b91a13f8d6` |
| Key | _(null in the export)_ |
| Epic | [RELAY](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | relay |
| Section | backlog |
| Tags | — |
| Created | 2026-08-15T10:44:16.425357+00:00 |
| Updated | 2026-08-15T10:44:16.425357+00:00 |
| Completed | — |

## Proof command

```sh
bash scripts/proof-check.sh 'go test -race -run TestCmdHasNoPlaintextListener ./cmd/agent-bus/...'
```

## Description

client/guard_test.go's roots are {".", "../cmd/agent-busctl"} -- NOT cmd/agent-bus. cmd/agent-bus has its own guard scanPlaintextListener (cmd/agent-bus/tlslisten_test.go:530) but it bans only InsecureSkipVerify and a set of flag names. The security gate MUTATION-PROVED that adding cfg.ClientSessionCache = tls.NewLRUClientSessionCache(64) by assignment beside tls.Client in cmd/agent-bus/relaydial.go builds and passes EVERY guard. No live defect today, but it is exactly the silent-pin-bypass shape invariant 11 exists for: crypto/tls does NOT call VerifyPeerCertificate on a RESUMED session, so a session cache silently disables pinning while every positive test still passes. Minimal fix: add ClientSessionCache to scanPlaintextListener's banned identifiers (cmd/agent-bus/tlslisten_test.go), and extend TestCmdHasNoPlaintextListenerGuardIsRedCapable with a synthetic case proving the new banned identifier can go RED.

From the security re-verification of RELAY-24-BLOCKER-EGRESS (85ae8b32-3a46-4e85-bdfe-ea29730670fb).

## Relations (authoritative)

> Authoritative, from the Spec Server's relations resource. `blocks` is inert
> metadata — it never changes a task's status, so the status shown is always the
> task's own field.


- **relates to** [RELAY-24-BLOCKER-EGRESS](../RELAY-24-BLOCKER-EGRESS--85ae8b32/task.md)

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-24-BLOCKER-EGRESS](../RELAY-24-BLOCKER-EGRESS--85ae8b32/task.md) — RELAY-24-BLOCKER-EGRESS: a bus SENDING a relayed message has no wiring at all -- relay.Ne… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
