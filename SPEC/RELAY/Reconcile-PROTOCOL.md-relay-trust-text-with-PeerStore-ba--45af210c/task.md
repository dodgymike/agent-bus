# Reconcile PROTOCOL.md relay-trust text with PeerStore-backed CrossBusTrust

| Field | Value |
| --- | --- |
| Public id | `45af210c-2520-4a3a-9444-31f8e020bab1` |
| Key | _(null in the export)_ |
| Epic | [RELAY](../epic.md) |
| Status | todo |
| Priority | P1 |
| Component | documentation |
| Section | backlog |
| Tags | — |
| Created | 2026-08-08T23:15:22.824020+00:00 |
| Updated | 2026-08-16T10:39:16.381775+00:00 |
| Completed | — |

## Proof command

```sh
! grep -qF 'no bus signing key and no certificate fingerprint' PROTOCOL.md && ! grep -qF 'registers nothing and the' PROTOCOL.md && grep -qF 'peerstore.go' PROTOCOL.md && echo PROTOCOL_RELAY_TRUST_RECONCILED
```

## Description

PROTOCOL.md §8.5 currently says no pin source exists and every relayed message is ErrUnpeeredBus. That was true before RELAY-17, but RELAY-17 now implements durable PeerStore-backed CrossBusTrust and the documentation gate updated CONTRACTS.md/CONTRACTS-ONDISK.md. Update only the stale prose to distinguish implemented durable trust from the still-unwired/no-route serving boundary. Preserve the no-TOFU and separate TLS-vs-signing-key rules. Do not claim relay HTTP is live. This must be a documentation-only task against the already-dirty PROTOCOL.md, coordinated with its owner.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [RELAY-17](../RELAY-17--817649ce/task.md) — RELAY-17: CrossBusTrust implementation + attestation travels in the relay envelope (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
