# CONV-DOCS: conversations in PROTOCOL.md, CONTRACTS-* and AGENT_PROTOCOL.md -- the whole-epic doc sweep

| Field | Value |
| --- | --- |
| Public id | `0abb8f8d-84dc-4fe1-b4b9-6a5fb10a6545` |
| Key | CONV-DOCS |
| Epic | [CONV](../epic.md) |
| Status | todo |
| Priority | P3 |
| Component | conv |
| Section | backlog |
| Tags | — |
| Created | 2026-08-15T08:56:21.071757+00:00 |
| Updated | 2026-08-15T08:56:21.071757+00:00 |
| Completed | — |

## Proof command

```sh
grep -q 'wal-entry-kind' CONTRACTS-ONDISK.md && grep -q 'conversation' PROTOCOL.md && grep -q 'conversation' AGENT_PROTOCOL.md && echo CONV_DOCS_OK
```

## Description

The whole-epic documentation sweep. This does NOT replace the per-task doc requirement -- invariant
7 requires each capability's `AGENT_PROTOCOL.md` entry and CLI subcommand in ITS OWN task. This task
covers what only makes sense once the epic is coherent.

Definition of done:
  1. PROTOCOL.md: the conversation object in the wire protocol and on-disk format sections.
  2. CONTRACTS-ONDISK.md: the conversation record; **and the `wal-entry-kind` reservation namespace
     table**, which this epic introduced -- state that it coordinates the STRING `wal.Entry.Kind`
     discriminator (previously uncoordinated, so two parallel agents could both pick `"conv"`), and
     that it was SEEDED RETROSPECTIVELY with 1=`"message"`, 2=`"seqfloor"`, exactly as
     `cursor-format-version` v1 and `store-record-version` were seeded. Values 3=`"conversation"`
     and 4=`"convmember"` are CONV's.
  3. CONTRACTS-HTTP.md: the conversation routes, and the per-peer tracking bound.
  4. CONTRACTS-CLI.md: the new subcommands and flags.
  5. AGENT_PROTOCOL.md: a coherent end-to-end agent narrative -- create, send by id, change
     membership, what a removed participant sees.
  6. DECISIONS.md: confirm every CONV decision task's ruling actually landed there.

**DOC-PROOF WARNING (this repo has been bitten):** a `grep`-based proof passes on an INCIDENTAL
match elsewhere in the file. Whoever completes this must confirm the proof is RED BEFORE the fix,
and should tighten it to pin the specific table row / field name rather than a bare word if any
incidental match exists. A proof never observed failing is not evidence that it can fail.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
