# CONV-VS-THREAD: resolve the governance conflict with COMMS-THREAD-TRIAL/COMMS-THREAD-FIELD -- operator-facing

| Field | Value |
| --- | --- |
| Public id | `c31d1c40-4be7-499b-b935-6b5e4129c064` |
| Key | CONV-VS-THREAD |
| Epic | [CONV](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | conv |
| Section | backlog |
| Tags | — |
| Created | 2026-08-15T08:53:07.534625+00:00 |
| Updated | 2026-08-15T08:53:07.534625+00:00 |
| Completed | — |

## Proof command

```sh
test -f docs/conv/VS-THREAD-DECISION.md && grep -qE '^RULING: (CONV_SUPERSEDES_THREAD_FIELD|COMMS_TRIAL_FIRST|INDEPENDENT)$' docs/conv/VS-THREAD-DECISION.md && grep -q 'COMMS-THREAD-FIELD' docs/conv/VS-THREAD-DECISION.md && echo CONV_VS_THREAD_OK
```

## Description

DECISION TASK. This exists because CONV was filed into an EXISTING deliberate decision, and an
agent must not quietly overrule it.

THE CONFLICT, stated plainly:
  - COMMS-THREAD-TRIAL (todo, P2) trials threading BY CONVENTION with NO new wire field, and
    measures whether that is enough.
  - COMMS-THREAD-FIELD (P3) is filed with status **`blocked`** ON PURPOSE, blocked by
    COMMS-THREAD-TRIAL, and its own status note says: "Adding a wire field pre-emptively is exactly
    the kind of un-measured assertion this epic exists to avoid."
  - The whole COMMS epic's charter is "establish by MEASUREMENT, not assertion".

A server-tracked, durable, minted conversation object is **STRICTLY HEAVIER** than the wire-level
`thread_id` field COMMS refused to add without measurement. So CONV, taken at face value, does by
operator instruction the more expensive version of the thing COMMS deliberately gated.

**This is not an agent's call to make silently.** Both readings are defensible:
  (a) The operator's instruction is direct product direction and supersedes a measurement gate that
      existed only because nobody had asked for the feature -- COMMS-THREAD-FIELD should then be
      marked superseded rather than left blocked.
  (b) The measurement is CHEAP and still informative: the trial would tell us whether the heavy
      object is needed at all, or whether convention plus the already-plural recipient list gets
      most of the value for none of the durable state.
  (c) They are independent -- conversations are about MEMBERSHIP and ADDRESSING, threading is about
      REPLY STRUCTURE, and both can exist.

Definition of done:
  1. docs/conv/VS-THREAD-DECISION.md with `RULING: CONV_SUPERSEDES_THREAD_FIELD` or
     `RULING: COMMS_TRIAL_FIRST` or `RULING: INDEPENDENT`, naming COMMS-THREAD-FIELD explicitly,
     with the reasoning and the observation that would retire the ruling.
  2. If the ruling supersedes COMMS-THREAD-FIELD, spec-keeper flips it to `superseded` via the API
     with a `supersedes` relation -- do NOT leave a blocked task silently contradicted.
  3. The ruling is REPORTED TO THE OPERATOR, not just filed. It is their decision to confirm.

Parallel-safety: design only.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [COMMS-THREAD-FIELD](../../COMMS/COMMS-THREAD-FIELD--35db4a7b/task.md) — Add a wire-level thread/reply field -- ONLY if COMMS-THREAD-TRIAL shows convention is ins… (blocked)
- [COMMS-THREAD-TRIAL](../../COMMS/COMMS-THREAD-TRIAL--3a7705b8/task.md) — Trial threading via convention (no wire field) and measure whether it's enough (todo)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
