# AGENT_PROTOCOL.md is missing the CLI-11 (key export-public) and CLI-6 (log) sections -- both shipped and documented in CONTRACTS-CLI.md, invariant 7's other half is not yet in main

| Field | Value |
| --- | --- |
| Public id | `4b51635d-336f-4f25-94c2-64c53578859d` |
| Key | _(null in the export)_ |
| Epic | [AGENTIF](../epic.md) |
| Status | todo |
| Priority | P1 |
| Component | docs |
| Section | backlog |
| Tags | docs, invariant-7, sequencing |
| Created | 2026-08-14T13:19:43.255554+00:00 |
| Updated | 2026-08-14T13:19:43.255554+00:00 |
| Completed | — |

## Proof command

```sh
grep -qi 'key export-public' AGENT_PROTOCOL.md && grep -qi '\bagent-bus log\b' AGENT_PROTOCOL.md
```

## Description

Filed 2026-08-14. `agent-bus key export-public` (CLI-11) and `agent-bus log` (CLI-6) are both shipped at commit b88fc0b, both gated PASS, both documented in CONTRACTS-CLI.md -- and both ABSENT from AGENT_PROTOCOL.md, the agent-facing surface invariant 7 requires in the same task as the capability.

WHY IT IS MISSING, NOT A DROPPED BALL -- the text exists, this is a SEQUENCING block, not a writing task: AGENT_PROTOCOL.md was deliberately held back from b88fc0b by the integrator. Its worktree carried both CLI-11's and CLI-6's sections correctly, but ALSO carried a third agent's INVITE-GATE paragraph asserting POST /v1/enroll accepts an invite_id+invite_secret pair -- which is FALSE at b88fc0b (invite_secret appears nowhere in internal/httpapi at that sha; the code is unstaged, under reviewer CHANGES-REQUESTED, covered by neither gate). Committing the file as-is would have put a dated, untrue wire-protocol claim in main -- the same shape as the 2026-08-07 DECISIONS.md incident this repo already has a named failure mode for. The integrator correctly refused rather than edit another agent's text out from under it. AGENT_PROTOCOL.md is being sequenced together with AGENT_LOG.md and DECISIONS.md, which are multi-claimant files for the same reason.

SCOPE: once the INVITE-GATE paragraph either lands for real (matching shipped code) or is pulled back out to match reality, commit AGENT_PROTOCOL.md with (at minimum) the CLI-11 and CLI-6 sections it already carries in the worktree. Do NOT write new text for CLI-11/CLI-6 -- confirm what's already there still matches HEAD (a few hours may have passed) and commit it. This is a sequencing/landing task, not an authoring task.

CONTEXT: this is the third time in one day this repo has dropped invariant 7's agent-facing-doc half in the same commit as the capability (see this session's other findings on RELAY-13/RELAY-13-FU-DOCS and MTLS-CLIENTCERT for the pattern). Each instance had a legitimate reason (file-boundary conflict, multi-claimant file, a stale-vs-shipped race) -- worth a standing note that 'ship code and docs in the same commit' collides with 'never commit an untrue claim' often enough here that the sequencing discipline (hold the capability commit narrow, land the doc separately once its file is clean) may be the more honest default for multi-claimant files, not a workaround.

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [CLI-11](../../UNASSIGNED/CLI-11--bf966c07/task.md) — CLI-11: export the bus signing public key from the operator CLI (done)
- [CLI-6](../../CLI/CLI-6--47001cb4/task.md) — CLI-6: log -- read the append-only audit log (metadata only; also absorbs the WAL-dumper… (done)
- [INVITE-GATE](../../INVITE/INVITE-GATE--05a5216d/task.md) — INVITE-GATE: POST /v1/enroll REQUIRES a valid invite and fails closed; invite consumption… (done)
- [MTLS-CLIENTCERT](../../MTLS/MTLS-CLIENTCERT--0bc7a2eb/task.md) — MTLS-CLIENTCERT: the client generates and stores its own TLS keypair + self-signed certif… (done)
- [RELAY-13](../../RELAY/RELAY-13--97f3f1b4/task.md) — RELAY-13: Enrolment registers the agent's messaging public key (done)
- [RELAY-13-FU-DOCS](../../RELAY/RELAY-13-FU-DOCS--7f3a4b80/task.md) — RELAY-13-FU-DOCS: three docs/comments assert the opposite of shipped RELAY-13 behaviour -… (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
