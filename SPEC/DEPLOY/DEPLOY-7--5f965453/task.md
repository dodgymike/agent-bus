# DEPLOY-7: gate docker build . in the normal check loop so a broken image build surfaces at commit time

| Field | Value |
| --- | --- |
| Public id | `5f965453-2911-4c17-aab0-eb1c56bd257d` |
| Key | DEPLOY-7 |
| Epic | [DEPLOY](../epic.md) |
| Status | todo |
| Priority | P2 |
| Component | deploy |
| Section | backlog |
| Tags | docker, ci, regression-guard |
| Created | 2026-08-15T14:15:34.931034+00:00 |
| Updated | 2026-08-15T14:15:34.931034+00:00 |
| Completed | — |

## Proof command

```sh
TBD by implementer -- must demonstrate the check goes RED on a Dockerfile/module-layout mismatch (e.g. temporarily reverting the COPY client/ line) and GREEN once fixed, per CLAUDE.md's RED-before-fix requirement
```

## Description

Nothing today builds the container image, so the Dockerfile rots silently. At d5018a6 `docker build .` FAILED: the builder copied only go.mod, cmd/ and internal/, and cmd/agent-bus/relaydial.go imports github.com/dodgymike/agent-bus/client, which lives at the module root precisely because invariant 7 forbids internal/. DEPLOY-6 fixed the immediate breakage with `COPY client/ ./client/`, but the class of bug recurs the moment another package lands at the module root and nothing catches it until someone tries to build the image months later.

Wanted: something that builds the image as part of the normal check loop (CI and/or a pre-commit/local script referenced from CLAUDE.md's Verify section), so a Dockerfile/module-layout mismatch surfaces at the commit that causes it, not later. Consider a lightweight `docker build -t agent-bus:ci-check .` step wired into whatever check harness this repo already runs (see CLAUDE.md "Verify" section) -- must not require network access beyond what the existing Dockerfile already needs, and must not silently pass on a build failure (see CLAUDE.md's VACUOUS-proof warnings).

Reference: DEPLOY-6 (found and fixed the immediate breakage; this task is about preventing recurrence, not re-fixing the same bug).

## Relations (authoritative)

> **NOT FETCHED** — real edges are UNKNOWN here, not absent. This tree was built
> with `--no-relations`, which skips one rate-limited request per task. Re-run
> `bash scripts/gen-spec-mirror.sh` (no flag, ~70s) to render them.


_Unknown._

## Referenced in description (derived, not authoritative)

> Derived by matching task keys, title prefixes and public-id fragments in free text.
> The export has NO dependency field, so this is best-effort and NOT authoritative;
> a real `depends_on` field is tracked by CONTEXT-SPEC-DEPS.


- [DEPLOY-6](../DEPLOY-6--e12b75cd/task.md) — DEPLOY-6: host-reachable Dockerfile CMD + THREE-BUS-DOCKER.md federated runbook (done)

---

_Generated by `scripts/gen-spec-mirror.sh` from the Spec Server. Never hand-edit; the server is the source of truth._
